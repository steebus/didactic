# Parity record

The one place that says where every feature stands on each platform. A
feature is not finished until its row here is right, and a row is changed
in the same commit as the code that changes it.

**Mobile runtime:** not yet scaffolded. When Phase 4 lands, record the Expo
SDK and React Native versions here.

**Shared packages, as at 2026-09-12:** `@didactic/core` (35 modules — 33,
plus `fsrs` and `clozes`) and
`@didactic/tokens` exist and are read by `apps/web`. The *Shared via*
column below names where a row's logic will live; for rows whose module
has shipped, it now names something real rather than something planned.

## Status words

| Word | Meaning |
| --- | --- |
| `built` | Shipped and working on that platform. |
| `partial` | Shipped with a stated gap. The gap is in the Notes column, not implied. |
| `planned (n)` | Not built; scheduled for Phase *n* of `PLAN.md`. |
| `n/a` | Deliberately absent on that platform, with the reason in Notes. |

The *Shared via* column names the package module that both platforms read,
or `—` when a row is rendering only.

## Surfaces

| Address | Surface | Web | Mobile | Shared via | Notes |
| --- | --- | --- | --- | --- | --- |
| `/enter` | Entry sheet | built | planned (4) | `api/auth` types | Phone signs in against Supabase directly; no claim flow on the phone. |
| `/` | Subjects | built | planned (4) | `core/shapes.HomeData`, `core/scoring`, `core/stock`, `core/specimens`, `core/copy.editionDate` | Margin blocks stack under the list on the phone (always below 60rem). Loose stock is not listed here: one entry under its own *Loose stock* head, set as a holding with the unfiled total where a subject prints its figures, leads to `/loose`. |
| `/graph` | The bed | built (Sigma) | planned (6) | `core/graph` encoding and layout constants, `core/filing.claimSentence` | Skia + graphology on the phone. Panel is a bottom sheet on both below 40rem. The panel carries **Filed under nothing** for a topic that sits on no bed: where the bed says it goes, with a one-press *File it there* (`components/WhereItLooks`, `TopicDetail.nearby`). This is the surface where the fault is actually visible — an unfiled topic is drawn pale, hanging off a coloured hull it is joined to five times over — and until now the only thing the panel could do about it was send the reader to another sheet. Filing re-reads the bed rather than patching the node, because it moves which hull the topic is drawn inside and which ink it is drawn in. |
| `/marked` | Marked, as a botanical plate | built | planned (5) | `core/shapes` highlight rows, `core/sections`, `core/timeline` (`gist`, `opens`, `tallyOf`, `STRAND_LABEL`), `core/specimens.STRAND_GLYPHS` | One date-ordered stream holding both kinds of thing: passages kept while reading, and diary entries about a week. They are the same history, and reading them on two sheets meant holding two sheets in your head to see one month. **It is now a drawn stem rather than an ordered list.** One line runs the length of the sheet with the days as stations on it — a ringed disc on the stem, the date beside it, a rule out to that day's tally — and every row hangs off it as a specimen: a leaf for a passage (`--plate-olive`), a bud for a note of your own (`--plate-mustard`), the thing in flower for an entry (`--plate-plum`), each on a hairline tendril. The forms are what the rows *are*, not decoration: the shape of a month can be read at arm's length before a word of it is. Geometry in `core/specimens.STRAND_GLYPHS` so the phone draws the same forms with `react-native-svg`. Every row is folded to its own opening words — `MARK_CLIP` 150 for a mark, `ENTRY_CLIP` 240 for an entry — and opens in place; the summary is the row's **own words**, clipped, never generated, and a row with nothing under it gets no control that would open it (`opens()` decides). *Open them all* folds the whole plate out for reading a season down. An open entry still takes `--paper-deep` with a plum rule rather than a card. Today and yesterday named, local days not UTC. Counted as marks and entries separately, at the head and per day. Note reader renders markdown natively.
| `/inbox` | Inbox | built | planned (5) | `core/shapes.PendingTopic`, `core/shapes.TopicEvidence`, `core/adjudication`, `core/shapes.LibraryRow`, `core/config.DUPLICATE_RESOURCE`, `core/copy.LABOURS` | First mobile sheet after the subjects sheet. **The library sheet was folded in here.** Two sheets stood over one table: the inbox asked what was waiting, the library asked what there was, and since the second answer contains the first they were mostly the same list printed twice -- with removal on one and marking-read on the other, so which sheet you stood on decided what you could do to a row. Now: unread and read sections, a search and kind filter once there are more than eight rows to lose something in, and a folded-away *Tidy up* holding the duplicate offer and removal. Read/unread throughout; sown/unsown was a second vocabulary for one enum. **The adjudication queue now sets both topics out as plates.** It asked over two names and at most one description — a decision that cannot be undone, put to the one person least equipped to answer it from wording alone — and now prints, for each side, its description (or *there is only the name to go on*, said rather than left as a gap), the subjects it is filed under, up to three of the resources it was drawn from by name, and what it holds. One line of counsel about *those two* replaces the same sentence printed under all twenty-five rows, and it never says *merge*. |
| `/loose` | Loose stock | built | planned (5) | `core/shapes.LooseTopic`, `core/loose.reckon`, `core/adjudication.holdings`, `core/filing.filingClaims`, `core/filing.claimSentence` | Every topic filed under no subject at all, dealt with in handfuls. The subjects sheet used to list them as names and now shows only a single entry with their total — a list of names is the wrong tool, because the question is never *what is loose* but *which of these are worth keeping, and where do the rest go*. Each row prints what it holds (the adjudication queue's own reading, for the opposite purpose: there it tells two topics apart, here it says what a delete would destroy), its description or *there is only the name to go on*, and what it was drawn from. Pick several, then file them under a subject, or throw them away behind a reckoning that counts what goes. Promote is per row. A topic carrying a route says so instead of offering a control `044` would refuse. Terracotta band: what is on this sheet is unresolved rather than kept. Each row also prints **what the bed already knew and nothing ever asked it**: the subjects its own neighbours sit in, as the counts rather than as a verdict — *Every one of its 5 filed neighbours sits in Shares and Stocks* (`core/filing.claimSentence`) — with a one-press *File it there* onto the same `subjects.fileTopics` the bulk control uses, so it carries the same caveat about *Draw connections*. The claim is `components/WhereItLooks`, shared with the topic sheet's own filing block and the graph's topic panel: three surfaces ask where an unfiled topic goes and each had a different amount of the answer, so one component states it and the sentence itself is in `core/filing` for the phone. A topic's subjects are settled when it is made and its edges are drawn in a later pass, so the evidence that would place it does not exist at the only moment anything looked: a topic could sit with five edges into one bed and belong to nothing, which the graph drew as a pale node hanging off a coloured hull. Above `config.FILING_SETTLED` a topic files itself when it is made and never reaches this sheet; what is printed here is the band below that bar, and the counts are shown because they are the reasoning — the reader is the one deciding. |
| `/subjects/new` | Sow a subject | built | planned (5) | `core/specimens` roots geometry, stage names; `core/books`; `api/curricula.draftAndOpen` | Roots gauge is a native slider driving the shared specimen. PDF proof via document picker. Takes `?from=<resourceId>` from fertile ground on the subjects sheet: the resource is filed as proof as the sheet opens, because that is what it is -- something already read on this subject, the same claim the proof section makes about everything in it. The subject is deliberately left unnamed, since only the reader knows what the subject around it is called and a title guessed off an article headline would be worse than the one they would type. A `from` naming a resource that is gone is ignored rather than an error: the sheet still sows. The sheet sets the bed's opening going from inside the sowing's own bench job, so it happens whether or not anyone is still standing in front of it — which is the ordinary case, since the sheet has just told them they may walk away. |
| `/subjects/[id]` | Subject bed (outline) | built | planned (5) | `core/tree.buildTopicTree`, `core/outline`, `core/progress` | Grub out, add topic, relate, resow. One flat list, not a tree: the nesting was inferred from whichever `specialises`/`prereq` edge was strongest, nothing checked the general topic was the parent (TypeScript printed holding JavaScript), and an outline admits one parent where the graph has several. The edges are untouched — `/graph` still draws them all and `044` still walks them — only the sheet stopped drawing one reading of them as depth. Topics run in the order the bed is in (`topic_subjects.position`), then attention-first. Position leads because the reader can now nudge a topic with ↑/↓: a list that reshuffles itself the moment a lesson is opened is not an order anybody can keep. Attention still decides between topics the bed never placed, which is where it was doing the real work anyway. A nudge writes one sequence over the whole subject rather than one per box, so a topic moved past a boundary lands in the next group. Both nudges reorder locally first and slide into place (`useSlide`, FLIP through the Web Animations API, honouring `prefers-reduced-motion`); the local order is released as soon as the server's answer changes, and dropped if the write is refused. Simplest-first is still what a sowing writes; a topic the bed never placed — added by hand, or in a bed sown before the order was kept — sorts last and falls back on what its lessons say. **Grouped** (`047`): the bed's second axis, subject matter across where the order runs complexity down. All of it lives under *Edit*: *Group these* proposes them in one model call over the whole bed and writes them, and the reader then renames, reorders with ↑/↓, moves a topic between boxes with its *Move to*, makes an empty group and removes one. Proposing is inside the mode with the rest of the arranging because it rewrites the boxes, which does not belong within reach of a reader who came to read the bed. Groups live on `topic_groups` and membership on `topic_subjects.group_id`, on the join for the reason `033` gave about `position` — a topic groups with the language fundamentals in one bed and the runtimes in another. **Not every topic is grouped**: null is ordinary, and an ungrouped topic is its own unframed band — unframed because "the rest" is a claim nobody made — interleaved among the boxes in the place the reader put it. Boxes and loose topics share ONE sequence: a group's rank is `topic_groups.position`, a loose topic's is its own `topic_subjects.position`, and `bandOrder` writes both halves from one list so they stay comparable (`group:<id>` / `topic:<id>` entries). Ties go to the box, which is the old data where groups were numbered 0..n separately. Ordering boxes by where their contents fell in the topic order was the earlier bug: the group nudge was a no-op, since the band stayed wherever its first topic sat. Removing a group is `on delete set null`, so it can never take a topic with it. The condition sort stays flat and ungrouped: what needs tending is a question about the whole bed. |
| `/subjects/[id]/reading` | The reading | built | planned (5) | `core/tree.readVerdict`, `core/specimens` | Two plates at the same size in the subject's ink. |
| `/settings` | Settings | planned (3) | planned (4) | — | Holds Close to begin with; a sheet that can grow. |
| `/topics/[id]` | Topic | built | planned (5) | `core/shapes.TopicArea`, `core/scoring.viabilityFigure`, `core/stock`, `core/progress`, `core/lessonState`, `core/voicing`, `core/figureRecord`, `core/graph.nearbyTopics`, `core/graph.edgeKindLabel` | Band in the subject's own plate ink. **Why this figure** is no longer a margin block: pressing *Viability* or *Condition* in the band unrolls it as a slip, and every line prints what it moved — the log replayed through `core/scoring.unroundedAbility`, so the lines add to the figure — with diary entries that recorded nothing listed as *moved nothing*. The replay asks for the figure **before it is rounded to the tenth `topics.ability` holds**, and prints hundredths where a line is worth less than a point. It used to read off the stored figure, and a tenth of ability is two and a half points of viability — more than a marked passage or a right answer is ever worth — so everything lighter than a read printed as a flat `±0` and a read printed as whichever of `+2` or `+3` the rounding landed on. A reader who marked passages and answered questions all week had a record saying all of it did nothing. A struggle is still the one line that genuinely moves no points, and still says *made it a guess* rather than a number. **Nearby** lists one row per topic rather than one per edge (`core/graph.nearbyTopics`): a pair can hold several edges and a `related` written from each end is two rows saying one thing, which printed the same name three and four times over and read as a fault in the list. The most definite claim wins — `prereq`, then `specialises`, then the two sideways kinds, with `related` last because any two topics in a bed can be said to be it — and the rest of the claims stay on the bed, which is the view that exists to show every edge at once. The caption is read from the right end of the edge (`core/graph.edgeKindLabel`): the two nesting kinds have a direction and mean opposite things at their two ends, and one table used for both told a reader to *sow first* the topic that in fact comes after. `TopicArea.record` carries it; the phone prints the same lines from the same function. Lessons carry a standing stamp, the route's *Up next* mark, and a listening ring (see *Listen to a lesson*). **Marked is folded shut.** It is the last section of the sheet and the longest -- a month of a topic is fifty passages of several lines each -- and it stood open by default between the route above it and the resources below, so the way down was a screen of quotations already read once. A `<details>` closed on load, its summary taking the same section head the sheet's other sections take, with the count still printed in it: the figure is what the reader wants most of the time, and a fold that hides it is one you have to open to read. The browser's own element rather than a button and a boolean, because the sheet is rendered on the server and `<details>` already carries the keyboard, the ARIA and find-in-page. Carries the **Filed under** block in its margin: every subject the topic sits in, which one is its home, *Take out*, and a picker with *File it here too* and *Move it here*. Where the topic sits on no bed at all, the block also prints **where the bed says it goes** (`components/WhereItLooks`, `TopicArea.nearby`) with a one-press *File it there* — the block used to say *loose stock, file it below* and then send the reader to a select box to supply an answer the graph was already holding. Membership has been many-to-many since `012` and no sheet could say so — a topic could be typed into a bed by name and taken out from the bed's own edit mode, and moving one meant retyping its name somewhere else and hoping the resolver reached the same row. Also carries **Its level**: make it a subject, or fold it into another topic as a lesson. Both refused while it carries a route, and the block says why rather than hiding the controls. Filing is confirmed optimistically — the placement behind it is a model call over the whole bed, and the decision was made when the button was pressed — with the new subject marked *settling* until the server catches up. |
| `/curriculum/[id]` | Curriculum | built | planned (5) | `core/curriculum.viewLessons`, `tierLessons` | Ultramarine band on both. |
| `/lesson/[id]` | Lesson | built | planned (5) | `reader` (the whole body), `core/blocks`, `core/sections`, `core/clozes`, `core/voicing` | The body is the same reader on both, in a WebView on the phone. Marking it read or worked plants its trackers on the bench; every passage already tended wears a plum rule, and a selection offers *Add mark* and *Make a cloze* side by side, each in the ink of the thing it makes. The clozes are read in a request of their own rather than folded into `/api/lessons/[id]`, so a client that has never heard of the garden gets exactly the lesson it always got, and a garden that cannot be reached costs the reader nothing but the highlighting. The **roster the prose resolves its names against is read the same way**, for a plainer reason: `links` and `sources` each fan out across every topic that shares a subject, and folded into the lesson read they were the whole of the sheet's wait — the reading sat behind a dozen round trips that print not one word of it. Split out (`/api/lessons/[id]/links`), the prose paints on the lesson's own read and its `lesson:` and `source:` names resolve underneath it; until they land they print as stubs, which is what a name that reaches nothing already printed, so nothing in the text moves when the real links arrive. `topic` and `requires` ride along on the reads beside them as embeds for the same reason: neither was ever worth a round trip of its own. The sheet itself is **read on the server**: it was a client component that fetched on mount, so the document arrived empty and the reading began a round trip later — the whole of the wait was a request the server could have made while it was rendering. `page.tsx` reads it and hands it to `LessonSheet.tsx`, which is still the same client component and still owns marks, the bench, the garden and writing on first open; it simply starts with its first read in hand rather than asking for it. `lib/lesson.readLesson` is that read, shared with `/api/lessons/[id]` so the sheet and the wire cannot drift. **Tend this lesson** sits at the foot of the reading, between the prose and *How did you go?* (046): one line of standing (*12 cards, 3 due*), a sitting over this lesson's cards drawn shuffled from what is already in hand, the whole deck listed to be read over, rewritten or pulled up, and *Write some more* — which **adds**, handing the model every question the lesson already asks and keeping only what it writes that is new. Nothing standing is ever deleted: a card answered for three months carries the only evidence of what the reader holds. The list is the one place in the app that prints a lesson's whole deck, and it can be a list where the Tend sheet cannot because it is an inventory to fix rather than a queue to work; a row prints the front only, with a cloze's blank drawn as a rule, and opens into the whole card rather than into a second set of controls. A card that gives away its own answer is marked *reads off* (047) — only a card planted before that rule existed can be one, and it is marked rather than deleted, because throwing away a card the reader has been answering for months, unasked, is not a correction the app gets to make. Two things about the furniture around the reading. The **rail across the top keeps the lesson's name on a phone**: under `34rem` the name used to go and the trail stay, which is exactly backwards for a reader who opens the app again on the train twenty minutes into a lesson and is told which topic they are in and not which of its fourteen lessons. The two take a line each instead, in the order they are asked in, neither allowed to wrap, and the name is set as the head sets it — same family, same axes, one step above the reading — where it had been the display face on axes of the rail's own and read as a second typeface rather than as the title said again. And the **marking desk's buttons travel the sheet rather than the reading**: they ride a sticky line, a sticky line rests at the end of the box it was laid out in, and that box held the prose alone — so they came to rest over the last lines of it and sat there for the whole of the tally, the rewrite, the garden, *How did you go?* and the way on. The sheet hands over the box that holds all of it and the desk is portalled in as its last child, so the line runs the length of the page and the buttons settle in the clear space under it. A third button joins them under the two that write: **back to the head of the sheet**, shown only once the reader is more than three quarters of a screen down. A lesson is the longest reading in the catalogue and its title, its trail and the way on are all at the top, so the return journey was a scroll the length of everything just read. It sits last in the column because it is the one control there that does nothing to the lesson — the two above put something down, this only moves the page — and because it is the one that comes and goes, so the two that are always there never move under a thumb reaching for them. Round where the rest of the catalogue is square, for the same reason. The threshold is a screen rather than a pixel: offering it to someone who has nudged the page by a line is offering to undo the nudge, and it is also what stops the button flickering around the top of the page. The scroll is smooth unless the reader has asked for less motion, this being the longest travel any control in the app performs, and the listener is `passive` so it can never be the reason a scroll stutters. It inherits the desk's own gate, so it never stands over a composer. |
| `/refresher/[topicId]` | Refresher | built | planned (5) | `reader` | Eyebrowless on both. |
| `/source/[id]` | The cited passage | built | planned (5) | `core/sourceLinks`, `core/passages.headingAt` | Where a citation lands. The passage in plum, with its page and the chapter it falls in, not a rendered PDF: what a reader wants mid-sentence is the sentence, and a viewer is the first thing that would break inside the phone's WebView. `?page=` selects it; a passage running over from the page before counts, so a citation never lands on a blank sheet. The document itself is one press away on a signed link (15 minutes) with `#page=`, which the browser's own viewer honours. Stepping is asked of the passage table rather than counted, because a page of nothing but a figure cuts to no passages and stepping onto it would show an empty sheet. |
| `/tend` | The garden | built | planned (5) | `core/fsrs`, `core/clozes`, `api/clozes` | One card at a time, never a list: a list of two hundred cards is a backlog, and a backlog is a thing to feel bad about rather than a thing to do. A card is one of three shapes (046) — a sentence with one to three words taken out, a question and its answer (or a term and its definition, either way round), or a statement to judge with one line saying why — and the three differ in the question and in nothing else: the same eyebrow, the same *Show it*, the same four rungs, so a reader meeting a true-or-false after a cloze meets a different question rather than a different instrument. The queue is **chosen** oldest-first and then **shuffled** before it is dealt: cards planted together were read together, and answering them in that order is answering each one with the last still in mind, which tells the scheduler the reader holds things they have only just read. The four answers carry the wait each would give — on a card met for the first time, *Gone · 10 min*, *A struggle · 1 d*, *Got it · 3 d*, *Easy · 15 d*, and all four growing as it is held (a card at a fortnight's stability, answered on time, offers 21 d, 2 mo and 4 mo) — computed on the page from the same pure scheduler the server will run, so the figure on the button is the figure that lands, and a reader chooses between consequences rather than between adjectives. All four rungs, because the weights were fitted against reviews graded on four: offering three would answer on a ruler the fit does not know. *A struggle* is a recall, not a softer miss — it lengthens the interval, just least of the three that do. Answering is optimistic and confirms nothing: the card goes on the press and the next rises into its place, with the write happening behind the reader. There is nothing left to confirm — the wait the answer buys was printed on the button before it was pressed, which is the only place it could change what the reader does. A write that fails says so in a sentence under the deck and does not drag the card back: the schedule was never moved, so the card is still due and comes round again. Edit and pull up sit on every card, both optimistic too, and editing never resets the schedule. The scope is on the URL (`?subject=`, `?topic=`, `?lesson=`, `?mode=random`), so tending one bed is a place that can be linked to — which is what the garden line on the stock list, a subject and a topic link to. Face down until *Show it*, and nothing of the back is in the DOM until then — including the concept's **gist**, which moved there in 047. It was printed above the question so an answer was recalled from something rather than guessed from nothing, but a gist belongs to the concept and is written about the concept rather than about any one card under it: no one sentence can avoid all their answers, and it handed one over. Showing it only when it happens not to leak would be worse, because absence is information. Where the card came from is a `Crumbs` trail at the end of the quiet row — the topic, then the lesson inside it — rather than two links side by side that read as peers and said nothing about which contained which; on the sheet a reader is meeting cards out of order from anywhere they have read, so that is the context they are missing. The concept's name still stands above the question, as a heading rather than a claim, and on the web it is now set as one: a card sizes everything inside it off four local steps, with the question the largest thing on it and the concept second, both clear of the label register the kind, the waits and the quiet row share. On the Tend sheet, where the card is the whole page, those steps and the column holding them grow fluidly with the viewport — a body to `1.75rem` inside `65rem`, about twice the area a laptop used to give it, where before it was a postcard on an otherwise empty desk. The card met inside a lesson keeps the catalogue's steps: it is a panel opened against a passage, and it should not be louder than the prose it interrupts. The phone will want the same ordering and none of the growth — a sitting there is already the whole screen. The answers are a grid rather than a wrapping row, so four never break to three and a lone fourth; on a phone they go two by two, because a mis-tap here schedules a card wrongly for a year and four stacked rows would push the last one below the fold. The card is dealt: two edges behind it while more than one is left, and each new card rising from just below with a hair of scale — the only motion on the sheet, all of it through `--motion-travel`, so reduced motion keeps the fade and flattens the deck rather than promising a movement nobody asked to see. |

## Capabilities

| Capability | Web | Mobile | Shared via | Notes |
| --- | --- | --- | --- | --- |
| Session: sign in, stay signed in, close | built (cookies and bearer) | planned (4) | `api/client` header provider | The gate takes either; the phone sends `Authorization: Bearer <jwt>` and is never redirected or refreshed server-side. |
| Foot bar (five sheets + Settings, with glyphs) | planned (3) | planned (4) | `core/specimens` glyph paths, `tokens.footBar`, the same six addresses | Replaces the running head's sheet links on both. Word under every glyph. |
| Running head: back, sheets, write | built | planned (4) | — | Four sheets — the bed, Marked, Tend, Inbox — then *Write an entry* behind a rule. **Sow and Close were taken out of it.** Sowing was never a sheet: it is something you do, and it is now an outlined control on the subjects sheet beside the Subjects title, where the decision to start one is actually made. Closing is one press on a single-user app with no second account, and it was taking a slot in the most-read row in the catalogue; it now sits at the foot of the subjects sheet as a quiet *Close the catalogue*. The subjects sheet no longer prints itself either — every other sheet marks where you are, but this row was three lines deep on a phone and the sheet's own title is already the largest thing on it. Between them these take the phone head from three lines to one. |
| Inbox tally on the inbox link | built | planned (4) | `api/inbox.count` | Stamped mustard on ink, same wording. |
| Listen to a lesson | built | planned (5) | `core/speech` (what is speakable, and where it is cut), `core/voicing`, `core/waveform` (what the control against a lesson is offering, and where a second of the lesson falls among its pieces), `api/lessons.listen`/`voicing` | A lesson read aloud, played from a bar docked at the foot of every sheet so the reader can walk the catalogue while it runs. **The audio is made on a machine of the reader's own, not on the web host.** Kyutai's pocket-tts runs there on CPU at about 1.28x realtime — faster than listening, slower than reading — and the machine has no inbound route, so nothing calls it: it polls `lesson_audio`, claims a row, and pushes mp3s to the `lesson-audio` bucket. That is what makes this work the same from localhost and from the deploy, since neither ever talks to it. Because generation outruns playback, the lesson is cut into paragraph-sized pieces and each is saved as it is made: the first lands in about twenty seconds and the player starts there while the rest arrive behind it. `core/speech` decides what is said — every fenced block is silent, the widgets because they are things to do with your eyes and the code because dictated punctuation is not learning — and the chunking is written on the queue row rather than repeated on the worker, so there is one rule for what a lesson sounds like and it is the one with the tests. Android's lockscreen controls and screen-off playback are the Media Session API on the web and want no native app; on the phone this becomes a real player (`expo-av` and a foreground service), which is the only part of the row that is genuinely platform work. Stale audio is never played: the voicing carries the hash of the body it was made from, and a rewritten lesson is voiced again. **The topic sheet plays its route**: every written lesson takes a play control whose ring fills as the recording is made, and pressing several queues them one behind another rather than all at once -- the machine voices one lesson at a time, and six rows in the queue would leave the reader watching the first for twenty minutes with no way to tell which was which. The line is held in the sheet, so it is lost on navigating away; what has already been asked for carries on, since that lives on the server. **What that control is offering now lives in `core/voicing`** (`listenOffer`, `canPlay`, `voicingProgress`, `listenLabel`), so the phone's route draws the same circle from the same rule rather than from a second reading of the five server states; `api/lessons` re-exports the standing's type from there rather than declaring its own copy. Three things about it were wrong on the web and are fixed with the move. **The states could not be told apart**: `ready` and `none` differed by `--ink-soft` against `--ink` and nothing else, so a route of sixteen printed sixteen identical grey circles whether or not any of them had audio behind it, and the only way to find out was to press one -- the rim is now *broken* where there is no recording and *closed* where there is, which is geometry rather than a shade and survives being glanced at. **A lesson still being voiced could not be played**: the press fell through to the queueing path, which refuses a lesson already underway, so the control was visibly working and did nothing at all when pressed -- `canPlay` is true on the *first* piece rather than the last, which is the whole reason the recording is cut into pieces. And **the poll never stopped**: the hook's own comment said it asked only while something was being made, but the interval was set on mount and cleared on unmount, so a topic whose lessons were all recorded -- or none of them -- asked the server the same question every five seconds for as long as the sheet was open. **The bar can be taken hold of.** Position ran along the top edge as a 2px line that reported and could not be touched, so a twelve-minute lesson with no way back to something said four minutes ago was a lesson you restarted. It is now a scrubber: the same hairline at rest, a bead that fades in on hover, focus and drag, and a `1.25rem` grab band over it -- the mark is two pixels, the target is twenty times that, and neither costs the bar any height, since the bench stands on it and a pixel here moves every notice in the corner. A native `input[type=range]` painting nothing, for the press, the drag, the arrow keys and an `aria-valuetext` that says `4:12 of 11:38` rather than announcing its own number. **A drag is not a seek**: the recording is a dozen files and a bar that sought as it went would fetch a new one each time the thumb crossed a piece, so the thumb moves freely and letting go is what asks for anything. The translation between the one bar and the dozen files is `core/voicing.placeAt`, with `secondsBefore`, `spokenLength` and `spokenClock` beside it -- the phone's player is a different player over the same pieces, and the clock was a private function in the web's component. A seek into a piece that is not loaded cannot write `currentTime` when it is asked for, since the element has no duration until the file's metadata lands, so the offset is carried and applied on `loadedmetadata`. The lockscreen gained the same two things: a `seekto` handler, and `setPositionState` over the whole lesson -- without it the notification read the element, which knows only the piece it is playing, so a twelve-minute lesson showed as forty seconds twelve times over. Still nothing to take hold of while a lesson is being made: a handle over minutes that do not exist yet is an offer that cannot be met. **The bar lies over the foot of the sheet and folds away.** It set `--foot-bar`, which the bench stands on and the marking desk stands on through the bench, so pressing *Listen* re-laid the foot of every sheet in the catalogue and pressing stop re-laid it back -- buttons moving out from under a thumb already reaching for them, in the middle of reading, because a recording started somewhere else. It is the one piece of furniture the reader turns on and off at will, so it sets nothing and overlaps instead; `--foot-bar` stays as the contract for anything genuinely docked, which this is not. What it costs is the overlap, and the answer to that is a press: a minimise control beside *stop* folds the bar to a `2.75rem` disc at the near corner, `--paper` on a shadow with the played fraction round its rim, and the recording carries on -- the `audio` element is never unmounted and never re-parented, since re-parenting one stops it, which on a phone reads as the app cutting out. The disc stands on `--bench-stack` the way the marking desk does, which is the player moving for something else rather than a second arrangement. A fold is about the recording in hand and not a setting: the next lesson opens as a bar. **The transport's marks are drawn rather than typed**: ⏮, ⏸ and ⏭ carry emoji presentation by default on iOS, so the pause button was Apple's bright orange glyph in the middle of a paper-and-ink catalogue -- and the variation selector that asks for the text form is honoured unevenly, leaving the mark at whatever size and weight the fallback font draws it in a row where four of them must match. As paths on a shared 14-square they are the button's own colour at one weight everywhere. The control is first on the bar rather than beside ✕, which is the one press here that cannot be taken back and sat in the corner a thumb reaches for without looking. **Two collisions at the foot, neither of them new, are settled the two different ways they want settling.** The mark composer docks across the bottom edge at `z-index: 5` against the player's 50, so the bar covered its *Keep* and *Cancel* for as long as both have existed: the player now stands aside for it (`standAside` on the player's own channel, called by the Highlighter while a docked panel is up) and leaves the disc, which stands on the panel through a published `--mark-panel` exactly as it stands on the bench through `--bench-stack`. Writing about a passage is the foreground job and a recording running behind it is not, so the player is what gives way -- by folding, so nothing of the composer's moves and nothing of it is covered. Standing aside and being folded are kept as two facts: collapsed into one, a composer that opened and closed would hand back a bar the reader had already put away. The marking desk is the other case and takes the other answer: its two buttons sit `--space-5` up, inside the bar's `4.5rem`, so they have always stood over the player's own controls, and they now read `--player-bar` -- the bar's measured height, published only while it shows -- through a `max()` that leaves them exactly where they were whenever there is no bar. Not `--foot-bar`, because the point of the second variable is that two buttons move rather than the foot of every sheet, and the bar's height already carries the phone's chin, so a sum would lift them a home indicator too high. **The lesson's own head says whether there is a recording**: the *Aloud* figure read *Listen* either way, so the one press in the catalogue that might cost eight minutes of somebody's machine looked exactly like the one that costs nothing, and a reader who asked for a reading and walked off could not tell on coming back whether it had been made. It now prints `LISTEN_WORD` (`core/voicing`) off the same `listenOffer` the topic sheet's ring is drawn from -- *Read it aloud*, *Being read…*, *Listen · 4/12*, *Listen*, *Pause*, *Try again* -- with the ring language brought down to a mark beside the word: broken for no recording, closed for one. It reads the list route through `useVoicings` rather than the lesson's own, which signs every chunk's URL: the head asks whether it can be heard, and pressing is what signs. **The lesson sheet follows along**: the passage being spoken takes a rule and a wash while it is read. Matched on words rather than on an index, because `core/speech` cuts the recording into pieces and the page is laid out in paragraphs and blocks, and the two need not line up -- which also means a chunk that is a block has nothing to mark, correctly. **The player wears a drawn voice** (`core/waveform`, `components/WaveMark`), in two places and the same mark in both: on the bar in front of the clock, where it costs none of the width the lesson's name wanted, and in the middle of the disc the bar folds down to. The disc wore a chevron up, which is the honest picture of *this opens* and the wrong thing to say at that size -- a chevron is what every collapsed panel in every app wears, and the fact worth carrying in something the size of a thumbnail is not that it opens but that a lesson is being read aloud behind it. The rim already says how far in. While the voice runs, five bars swing between `WAVE_LOW` and full on one duration with their own offsets, so the row moves without travelling: staggered rather than marching, since a left-to-right sweep is what a progress spinner does. Stopped, they hold a silhouette -- tallest in the middle, falling away unevenly either side -- so the mark reads as sound standing still, paused and under a reader who has asked for no motion. Scaled rather than re-laid, a transform being composited where `height` and `y` are layout, for a mark that runs in the corner of a sheet for twelve minutes at a stretch. Geometry and timing in `core/waveform` with `core/tests/waveform.test.ts` behind them, so the phone draws the same voice rather than a second one that drifts. |
| The bench: work in hand, reported site-wide | built | planned (4) | `core/jobs` | Sowing a subject and writing a lesson each take about a minute and were held by the sheet that started them, so walking off orphaned the request and the answer arrived to nobody. The job is registered above the router (`components/Bench.tsx`, mounted in `layout.tsx`) and reported from the corner of every address: what is underway, then what came of it and the way there. Keyed by kind and id, so the topic sheet and the lesson's own sheet cannot both write the same body. Polite live region, never assertive. A notice carrying a link waits to be followed or dismissed; only one with nowhere to go puts itself away. It cannot survive a reload — the request belongs to the document — which both jobs are recoverable from. The phone gets this free from a native stack, so its row is the words, not the mechanism. A running job says what it can: writing counts its rounds and words, sowing gets `copy.LABOURS` as an explicit rumour rather than a bar that cannot know, and the third kind, `opening`, is a rumour (`copy.OPENINGS`) only while the route is being drafted and a measurement from there on. `opening` is the one job nobody pressed a button for, so it reports once — when the lesson is written and there is somewhere to send them — rather than at each step. The bench also carries **offers** — a notice that asks rather than reports, used to offer the next lesson once a reader is half way through this one. It no longer climbs over the reading to clear the reader's floating buttons: on a wide screen the two take opposite corners — the bench at the left, the desk at the right — and neither has to know how tall the other is. On a phone, where a notice runs the full width and there is no corner to take, the relation is **inverted** from what it was: the bench **measures itself** into `--bench-stack` and the desk's buttons stand on top of it, rather than the notice measuring the buttons and climbing. A `ResizeObserver` keeps the figure in step as notices arrive and leave, the property is set only while something is actually showing, and where there is no observer the one measurement still stands. |
| Filed-under line | built | planned (5) | `components/Crumbs` | A breadcrumb or nothing; never invented. One component on every sheet that has ancestors — Subject › Topic on a lesson, Subject on a topic — because a reader who learns to read the trail on one sheet should not have to learn it again on the next. The separator is drawn by the stylesheet on a wrapper rather than written between the links or put on the anchor's own `::before`, so it is never selected with the text, never read out as a word, and never underlined as though it were part of a link. The trail inherits the band's ink, so the same component prints in the green of a lesson head and in a subject's own plate. The lesson's `subject` rides along on the curriculum embed one hop past the topic; a topic filed under none gives null and one filed under several gives the first, because the trail says where the sheet sits and it sits in one place. |
| Viability figure, with *about* under 0.4 confidence | built | planned (4) | `core/scoring.viabilityFigure`, `core/scoring.vagueFigure`, `core/config.CONFIDENT_ENOUGH` | Italic, dimmed, caveat sentence, on both. The 0.4 used to be spelled out in all five surfaces that draw this state (`/`, `/subjects/[id]`, `SubjectBed`, `/topics/[id]`, `GraphCanvas`) — a threshold written five times is a threshold that moves in four of them. Now one predicate over one constant, which is also the line a struggling topic is held under, so the clamp and the sheets cannot drift apart. |
| Condition bar: hatch, word, label | built | planned (4) | `core/stock` | Three carriers on both; colour is the fourth. |
| Emblems | built | planned (4) | `core/specimens` silhouettes | `react-native-svg` on the phone. Forms must read at 48px on both. |
| Roots gauge and specimen | built | planned (5) | `core/specimens` geometry, stage names | Stroke drawn on through dash offset on both. |
| Route progress chip and specimen | built | planned (5) | `core/progress` | Word is the carrier; tick and colour redundant. |
| Lesson standing stamp, and *Up next* | built | planned (5) | `core/lessonState` | The route chip's question asked one level down: **Not written**, **Ready**, **Opened**, **Started**, **Worked**, least-worked first, on the route chip's own colour ladder — the mustard arriving as an outline before it arrives as a ground, so the two filled stamps stay the two that mean work happened. **Opened** (`036`, `lessons.opened_at`) is the rung that used to be missing: *Started* rests on marks, which under-reports badly, since a reader can read a lesson closely and mark nothing and leave their route looking untouched. Recording the open fixes that at the source rather than by loosening what *Started* means — having been in a lesson and having worked in one are different things, and a reader deciding where to go back to wants to know which. Written on the first open and never moved (`opened_at is null` is in the update itself, so two sheets racing cannot move it), fired on every open and dropping cache only when a row actually changed. Neither rung moves *Up next*, which stays exact on position and completion: opening a lesson and wandering off is not progress through a route. Word is the carrier; tick and colour redundant. `started` rests on marks, which under-reports by construction, so *Up next* — the first unworked lesson in the route — is what answers "where am I", from position and completion alone. A draft route has no next. `has_body` is a stored generated column (026), so the sheet never carries sixteen lesson bodies to print one bit each. |
| Add a resource: link, book, note | built | planned (5) | `api/resources.add`, `api/books.search` | One field and one press for a link. |
| The inbox says where a resource has got to | built | planned (5) | `core/filingState` | Everything between a resource arriving and being filed was invisible: a row printed *added 14 Sept* whether it had been filed against twenty topics, was still in the queue, or had failed three times and given up. The three look identical if the only thing printed is a date. The ingestion job is now read with the shelf and the row says which — **Waiting**, **Reading it**, **Filed · n** with every topic named and linked, **Filed under nothing** (read, and it matched nothing you are growing — not a failure, and the note says so), or **Could not read it** with the worker's own reason. Each state that could be misread as a fault carries a sentence answering *so is something wrong?*. The word is carried by a rule in the plate ink as well as by colour, since colour alone cannot encode state anywhere in this build. |
| Ingestion actually runs | built | n/a | `supabase/functions/ingest`, `core/filingState` | **It never had, in production.** Five stacked faults, each hiding the next. (1) The queue was jammed: a message named a resource since deleted, the worker asked for its job row with `.single()`, ignored the error, and then every update matched nothing — so attempts never moved, MAX_ATTEMPTS was never reached, and the message was never deleted. pgmq is ordered, so one dead message blocked every resource added after it, silently. (2) `APP_URL` and `INTERNAL_KEY` were unset on the deployed function and documented nowhere; `INTERNAL_KEY` did not exist in any env file, and the route refuses every request without it. (3) `jsdom` could not load on Vercel at all: `@exodus/bytes` is ESM-only and `html-encoding-sniffer@5+` and `jsdom@27+` both reach it with CJS `require()`. Held at `jsdom@26.1.0`, the last version whose tree is clear of it. (4) `extractConcepts` cast the tool result instead of reading it, and the model does not always answer in the shape the schema asks: `concepts` arrives as a string, sometimes serialised **twice**, sometimes truncated, and `summary` is sometimes absent. Now parsed, unwrapped and salvaged, with one malformed field costing that field rather than the resource. (5) A reading that found nothing returned 200 and reported success, so the inbox said *Filed under nothing* — the phrase for an article about something genuinely new — for one that was never read. It throws now, naming what it read and why the reading was empty, and the worker retries it. Verified live: five backlogged resources filed with 24, 11, 6, 32 and 15 topics; queue idle. |
| A read resource files itself into the subject it is about | built | planned (5) | `core/filing.filingClaims`, `core/config.FILING_SETTLED`, `core/resolution`, `core/config.JEV_LINK` | Ingestion always filed the resource against the topics its concepts matched (`commit_ingestion`, 012) but created the *new* topics belonging to no subject at all: a link about Rust ownership matched "borrow checker" and "lifetimes" under a Rust subject, then left "Pin/Unpin" an orphan on no subject sheet, reachable only through the graph. `039` files the new topics into whatever subjects the matched ones already sit in. Agreement is the whole test and it is read off this very resource's own matches, so "mostly about Rust" is a fact about the material rather than a guess at the title. Deliberately **not** a similarity threshold of its own — the resolver has already decided what counts as a match at a bar tuned against real rows, and a second number here would be a second opinion about one question, drifting from the first. A `pending` topic is never filed: it is the one the resolver could not decide about, and adjudicating it later would mean unpicking a membership it was never meant to have. Nothing matched means nothing to agree on and nothing is filed — which is what leaves fertile ground fertile. **`045` files by reading.** Agreement filed nothing when no name matched, however plainly a book's topics belonged to a subject already sown, and filed every new topic into every subject a match touched when something did. The extractor now writes a general description for every concept, stored as the topic's summary (and given to a matched topic that has none). One more model call (`lib/llm/overlap.ts`) reads each concept's description against its five nearest topics — with their descriptions and the subjects they sit in — and against every subject with a sample of what it holds, and answers two things: is this one of those topics, and which subjects does it belong under, if any. `settleResolution` weighs that against the embedding: a link above `RESOLVER_MATCH` stands; in the ambiguous band a sure *same* links and a sure *different* creates, where both used to queue for adjudication; below the band a *same* the names disagree with is asked, never linked. The subjects it names are sent per topic as `subject_ids`, and an empty list — read, and stands alone — is filed nowhere. The embeddings themselves are still of names only, because the resolver's bars are calibrated against name embeddings and every stored topic is one. A reading that fails, comes back truncated, or would leave too little of the minute for the edge pass (`JUDGING_NEEDS_MS`) sends no key, and `045` falls back to `039`'s agreement rule; the ingestion result carries a warning saying so. Same signature as `039`, so the web and the migration are safe in either order. **And a third reading, off the bed itself.** Both rules above run inside `commit_ingestion`, and the topic's edges are drawn in a pass *after* it, because the model needs real ids to relate — so the evidence that best places a topic does not exist at the only moment either rule looks. A resource whose concepts matched nothing by name left every one of them loose however plainly they sat among a subject's topics, and the graph drew the result: a pale node hanging off a coloured hull, joined to it five times over and belonging to nothing. `lib/filing.fileWhatTheBedIsSureOf` runs once the edges are written and files a topic under every subject that clears `FILING_SETTLED` — three of its filed neighbours, and a clear majority of them. Measured on the real bed rather than assumed: at three agreeing there are no wrong answers, at two there is one (*Data Science* into *Web Development*) and at one there are two, all of them the same shape — a broad topic that is its own subject and merely adjacent. The bar sits where the errors stop rather than where most of the filing is, because a topic left loose is on a sheet that exists to list it while a topic filed wrongly is quiet inside a bed somebody trusts. Never fatal: a filing that throws leaves the topic loose and adds a warning, rather than losing the reading. **And it is never a sweep.** Taking a topic out of its last subject is a deliberate act that deletes the row and leaves no trace, so a pass over existing loose stock could not tell that from a topic that was never filed and would quietly put back everything anyone had ever taken out — the settled claim is acted on only where the app itself has just made the topic loose. Everything already sitting there is offered on `/loose` instead. **And the cosine no longer decides.** `045` gave the reading a vote and `settleResolution` arbitrated between it and the embedding, which was one vote too many: `config.ts` already recorded that no cutoff separates "React / React Hooks" at 0.916, which must never merge, from "CDN Distribution / Content Delivery Network" at 0.852, which should, because gte-small scores shared vocabulary and every topic under one subject shares all of it. The bands overlapped, `RESOLVER_SIBLING_AMBIGUOUS` was a patch over one symptom of it, and `SURE_ENOUGH` gated on a `confidence` number a generative model wrote about itself — not a probability, and so not a bar over anything. The embedding now only **nominates**, which is the job nothing else can do: Jev has no index and must be handed its options. `lib/llm/jev.ts` asks `typesafe-ai/jev` through the Vercel AI Gateway — one shared state carrying the resource, its concepts and every subject, and two choice questions per concept whose criteria carry that concept's own candidates — and gets back a distribution over them. `core/resolution.readDistribution` reads it: a link needs `JEV_LINK`, and because the distribution is normalised that one bar already forces daylight behind the winner (a separate margin threshold was written, tested and found unreachable). `readSubjects` takes every subject above `JEV_SUBJECT` rather than the winner, so a topic splitting 0.45/0.45 lands in both beds — which is what many-to-many membership has meant since `012` and what an array from a generative model could never say. The shortlist widened from five to `RESOLVER_NOMINATED`, measured rather than argued: on this map the true topic was found at depth 5 for 95.5% of aliases and at depth 15 for all of them, so retrieval was never the main fault and the case for the demotion rests on the judging. `RESOLVER_MATCH` and `RESOLVER_AMBIGUOUS` survive as the **degraded path** — with no gateway, no key, or too little of the minute left, `settleWithReading` falls through to `resolveConcept` and the inbox carries the warning saying so, which is the only reason a resource can still be filed with no model call at all. `scripts/bakeoff/` holds the harness that measures the two against each other on probes written from the real map: aliases that must resolve home, and deliberately close neighbours that must not. `lib/llm/overlap.ts` is kept as the harness's control arm and is no longer on the ingest path. |
| Fertile ground on the subjects sheet | built | planned (5) | `core/shapes.FertileGround` | What was read, put topics in the ground, and matched no subject already sown. These topics existed before this — as *Loose stock*, a bare list of names with nothing saying where they came from or what to do about them. Named together with the resource that produced them, the same rows read as a proposal: here is a thing you kept, here is what it is about, none of it is ground you hold. *Sow a subject from this* opens the sowing sheet with the resource already filed as proof — which is exactly what it is, something already read on the subject. The subject is left unnamed on purpose: only the reader knows what the subject around it is called, and a title guessed off an article headline would be worse than the one they would type. Built from the reads the sheet already makes, so it costs no round trip. *Loose stock* stays for topics sown by hand, which belong to no resource. |
| Share a URL into the inbox from another app | n/a (browser share target not built) | planned (5) | `api/resources.add` | The phone's reason to exist. A web share target is a possible follow-up. |
| Upload a PDF or a book | built | planned (5) | `api/resources.uploadDocument`, `core/documents` | No longer multipart. The platform refuses a request body over 4.5 MB before the handler runs, so the 15 MB the old route advertised was a ceiling it could not keep — a certificate squeezed through and a textbook died at the platform with an error the app never saw. The browser asks for a signed URL, PUTs to storage itself, and tells the API afterwards; 50 MB. The phone does the same three steps through the one composed function, so neither front end has to know why. `api/resources.upload` stays for a build that has not been rebuilt, with its ceiling corrected. |
| Progress while a document is read | built | planned (5) | `core/copy.READINGS`, `core/copy.labourPhrase` | A file input has no busy state of its own, so a book going up and being opened — the longest a reader waits anywhere here without a word — used to pass in complete silence. Its own list of phrases, in the register of handling a book rather than turning soil, polite live region. `LABOURS` also grew from twelve to twenty-three: at 2.6 seconds a phrase, twelve wrapped in thirty-one seconds and left a reader watching "Almost done" for the second half of a sowing, which reads as stuck rather than as nearly there. A test now holds the list to the length of the wait. |
| A document read into citable passages | built | planned (5) | `core/passages` | Cut on paragraphs, never mid-sentence, each carrying the pages it came off — that page is what a citation prints, so it is the whole point. The outline is read on the upload (`ensureOutline`, idempotent) and the passages in rounds against a cursor on the job (030), because a book does not fit in the sixty seconds a function gets and because the queue is a once-a-minute cron — left on it, the structure always arrived after the reader had already chosen a fidelity and sown; the round sizes itself on what a page of *this* document actually cost last time. Bytes are range-fetched from a signed URL, so a round pulls its pages and not the book. Embeddings are a separate pass: the edge function holding the model can be down while the parser is fine, and a passage without a vector is still readable and still citable by page. |
| A document opened in the app | built | planned (5) | — | `/resources/[id]/read`: the file in the browser's own PDF viewer, in a frame of the catalogue's own. The viewer is the browser's because it already has page navigation, zoom, search and print, and rendering the pages here would mean carrying pdfjs into the browser to rebuild controls that are already there. The URL is signed on the server and the bucket stays private; a `#page=` fragment is read in the browser and put on the frame's address, so a citation of page forty opens at page forty. Reached from the inbox and library rows, and from *Open the document itself* on the citation sheet — which used to hand out a raw signed link in a tab of its own. The phone opens documents in its own viewer, so its row is the way in, not the frame. |
| A PDF filed from the inbox | built | planned (4) | `api/resources.uploadDocument`, `core/documents` | The same three-step upload the sowing sheet uses — sign, PUT, tell the API — because a book is far over the four and a half megabytes a request body may carry. Kind, size and the 50 MB ceiling are checked in the browser before the bytes move, off `core/documents`, so a wrong file is refused without a round trip. Choosing the file is the press: no *File it* button, because a disabled one beside a picker reads as a step still to come. The answer says what was found in it — how many chapters, or that none could be — which is what a reader wants to know about a document they have just handed over, and which waiting for the queue means never saying. |
| Mathematics in a lesson | built | planned (5) | `lib/maths` (web), `core/clozes.mathSpans`; the phone reads the same rendered HTML | TeX between `$` and `$$` is typeset by KaTeX at render time. **MathML, not KaTeX's HTML**, and the reason is the allowlist: KaTeX's default output is a tree of spans carrying inline styles, and admitting it would have meant admitting `style` on every element in the prose — exactly the hole `markdown.ts` exists to refuse. The MathML tags are only ever mathematics; they carry no URL, no script and no ink, so the sanitiser widens by a bounded set that a model cannot reach through, and no stylesheet or font files ship at all. The tag and attribute lists are measured off KaTeX's own output over the notation a lesson actually uses — powers, fractions, roots, sums, limits, matrices, cases, accents, braces — rather than remembered, because a guessed list is a formula silently gutted into the text inside it. `mathcolor` and `mathbackground` are deliberately omitted. KaTeX is given no `trust`, so `\href`, `\url` and `\includegraphics` are refused at the source as well. Tokenised as a `marked` extension rather than as a pass over the string, which is what keeps a `$` inside a code fence a `$` — the same reasoning that resolves `lesson:` in a renderer. A formula KaTeX refuses prints verbatim, delimiters and all: a hole where an equation should be teaches less than the equation as typed. The block rule refuses a `$` inside a formula, because a lazy rule satisfies itself by swallowing a run of three display equations jammed onto one line into one formula that then fails — which is how a whole line came to print as raw TeX. Both painters walk the set formula and skip only its `<annotation>` — that carries the TeX it was set from and is rendered nowhere, so walking it would splice the source through the text a passage is searched for. A stored passage is looked for twice: as stored, then with its notation typeset. An agent's cloze quotes the lesson *source*, where an equation is still `$2^x = 100$`, while the page holds it *set*, where it reads as its glyphs — searched only as stored it was never found and the plum was never drawn. The second reading is produced by typesetting each formula in the quote and reading the text back off the result, so it is exactly what the page holds rather than an approximation of it. Block fields and the cloze card render through `renderInline` on a much shorter allowlist — emphasis, code and mathematics, no headings, lists or links. A blank may take a whole formula or none of one (`clozeProblem`, enforced on the reader's own clozes and the agent's alike from the same function): the card draws the passage in three pieces and typesets each, so a blank cutting through an equation would leave two halves that can only print as the raw TeX they have become. |
| A flow drawn as a flow | built | planned (5) | `core/flowGraph` (`flowGraph`, `flowPath`, `flowNodeSize`), `core/blocks.straightenFlow` | **A drawn graph since 049, not an arrangement of boxes.** It was CSS lanes with the rules built out of pseudo-elements, chosen so the lanes could stack on a phone and still read as a flow where a routed SVG could not. The stacking was worth having and the drawing was not worth keeping: a rule drawn on a box cannot reach a box in another column whose position it does not know, so lines stopped short of what they pointed at, the gathering rule found the wrong middle, and where two lanes ran to different depths an arrow fell out of the gap at nothing. Positions now come from dagre (~40KB, web only) and the whole of what it is asked for is coordinates: the boxes, the rules with their corners taken off, the heads and the labels are all this app's own ink. Laid out on the server as well as the browser, off worked-out box sizes rather than measured ones, so there is nothing to correct after hydration. Below its own width it scrolls rather than stacking — the answer `chart` already gives. Pressing a step lights the way in *and* the way out of it, since a reader stopped mid-decision may be asking either; what is off the path stands back rather than disappearing. `core/flowGraph` holds the part the phone will need: the tree a block is written as, flattened into nodes and edges with every branch end joined to what follows the parting, plus the reachability the highlighting reads and the box sizing the layout runs on — a graph built twice is two graphs that can disagree about where a branch rejoins. Only the layout and the drawing are web's. `straightenFlow` still fixes the one mistake the model actually makes — a step phrased as a question with no branches under it — by folding it into the step it was asking about rather than drawing an empty box above the answer. |
| A fresh sowing lands on the reading | built | planned (5) | `core/jobs.jobWay` | The bench's way on, the partial panel's button and the sheet's own navigation all go to `/subjects/[id]/reading` rather than to the bed. It used to go there only when the model had filled in the optional assessment part of its answer, which made the sheet that explains what was just done the one sheet a reader usually never saw. The reading stands either way — every figure reads through `sowing?.` and prints *unstated* where there is nothing — and it is the sheet that answers "where did this bed come from", which is the question anyone has the moment a bed they did not write appears. |
| A document's own chapters, resolved to pages | built | planned (5) | `core/passages.OutlineEntry`, `core/documentBed` | From the PDF's bookmarks where it has them — exact, free, the author's own page numbers. `pdf-parse` surfaces the outline but not the pages its destinations point at, so `pdfjs-dist` (already underneath it, same pinned copy) resolves them. Three ways, tried in that order, and `resource_outline.source` records which one answered because they are not equally trustworthy. No bookmarks: the front pages are read and the contents transcribed by the model. No contents either: the structure is inferred from typography (`core/headings`) — the lines set larger than the body text, ranked by size, which is what an article exported from a newsletter has instead of chapters. That last one costs nothing and guesses nothing, since the sizes are facts about the file; what it cannot read is a document that gives its headings no typographic distinction at all, and that is reported as having no structure rather than having one invented. A size used only once is a title and not a level — the document's own, or a second article's further in — and is dropped, because kept it would make the whole file one chapter with everything nested inside it. Scanned PDFs are out of scope and fail with a sentence. |
| How closely a bed follows the document it grew from | built | planned (5) | `core/documents.FIDELITY_RUNGS`, `core/documentBed` | Three rungs under any PDF filed as proof, and not a slider: each is a different path through the code, so a scale would promise an interpolation that does not exist. *To the letter* does not ask the model what the topics are — it tells it, one per chapter in the document's order, and asks only what each is called; the resolver still names each concept, because a topic is shared across every subject it sits under and "Getting started" is not something anyone can know. That caveat is printed on the sheet rather than left to be discovered. *Follow its order* seeds the bed and permits merging, splitting and adding. *For the ground it covers* takes the coverage as context and the arrangement as a hint. Not at all needs no name: it is what filing has always done. A bed laid out to the letter draws its edges from the outline and skips the model's edge pass entirely — the author already said how the parts relate. Which rungs are offered depends on what is in the document (`core/documents.rungsFor`): two of the three follow a shape, and an article, a paper or anything exported from a blog has none, so those two are not offered rather than offered and quietly ignored. The sheet prints what was found — the chapter count, or the reason there is none — because the structure is read on the upload itself, before the choice is made. |
| Mark a resource consumed at a depth | built | planned (5) | `api/resources.patch`, `core/config.DEPTH_WEIGHTS` | |
| Spaced repetition: the FSRS scheduler | built | planned (5) | `core/fsrs` | FSRS-5, the nineteen-weight version, pure and in `packages/core` with 26 tests — so the phone schedules a card exactly as the web does rather than to its own taste, and so the sheet can ask what an answer would cost *before* it is pressed. Nothing in it reads a clock: the caller says what `now` is, which makes every case a fixed sum rather than something that has to be waited for. Two numbers per card, stability (how long the memory lasts, in days) and difficulty (1–10), and a power-law forgetting curve rather than the exponential the older schedulers use — measured, memory has a far longer tail, which is why an exponential keeps asking for things the reader plainly still holds. The constants are chosen so that retrievability is exactly 0.9 after one stability, which is what makes stability readable as *how long this lasts*. A miss returns the card in ten minutes and never more than ten: a ladder of learning steps belongs to a drilling app, where a sitting is half an hour; this is a garden tended for two minutes, and a card back in sixty seconds is answered from short-term memory and teaches nothing. A same-day second look uses FSRS-5's short-term formula rather than the ordinary one, which would read an hour as a near-perfect recall of a two-week-cold card and pay in full. `DEFAULT_WEIGHTS` is the published fit; `review` takes a set, so a fit against this reader's own `cloze_reviews` drops in later without anything else moving. |
| Tending: concepts and cards from a worked lesson | built | planned (5) | `core/clozes`, `core/clozes.wantedVerdict`, `api/clozes.tend`, `core/jobs` (`tending`) | Marking a lesson **read** or **worked** — not skimmed, which is by definition not an exposure worth asking about — sets a bench job going that reads the body back for the two to four concepts it taught, with **one or two cards under each**. It was two to four, and a lesson came back with a dozen cards or more — a backlog rather than an evening's tending, and a queue nobody can face is a queue nobody answers. A second card still earns its place where a concept has two different things to ask; the third and fourth were the same question in another shape. The floor came down with the ceiling: a concept carrying one answerable card is planted, where before it was dropped whole, because a floor of two against a ceiling of two would lose every concept whose second card happened to fail a rule. **Cards are written from the lesson rather than cut out of it (046).** Until then every passage was verified verbatim against the body and one the model had tidied was dropped rather than repaired — sound reasoning that produced bad cards, because a lesson does not write in sentences shaped like questions: what came back was whichever sentence happened to be quotable with whichever clause happened to be removable taken out of it, so blanks ran to eight and ten words and the answer was a paraphrase nobody could have produced or checked. The model now picks each card's shape to suit the material — a cloze of one to three words for a term or a named quantity, a question and answer (or a term and its definition, either way round) for a definition, a true-or-false with its one-line reason for a claim the reader has half-absorbed — and writes the sentence the concept deserves. What survives of the verbatim rule is the optional `anchor`: the lesson sentence a card came out of, still checked character for character and dropped rather than repaired, which is what the plum wash in the reading is drawn on. A card whose anchor fails is kept and merely not drawn. A blank over four words, a true-or-false with no reason, and a concept left with no answerable card at all are all dropped — as is **anything that hands over its own answer** (047): a question containing it, a cloze whose blanked words are left standing elsewhere in its own sentence, or a concept whose *name* gives it away, since that name is printed above every card under it and was chosen once for every answer under it. A nudge that answers is stripped and its card kept. A card that can be read off is graded *Easy*, honestly, and filed away for four months on the strength of a reading, so a missing card is much the cheaper failure. Idempotent, which is what stops marking worked, un-marking and marking again from filling the garden with duplicates. *Write some more*, under the lesson, is the other way in, and it **adds**: the model is handed every front already standing and writes what is missing, anything matching one is dropped, and a concept it names that already exists is reused rather than written twice. **Nothing standing is ever deleted** — the old `regenerate` replaced a lesson's concepts and cards outright, which meant the only route to better cards ran through throwing away the review history of the ones you had, and that history is the only evidence of what the reader holds. It is a job rather than a wait because the reader has just pressed the button that says they are finished with this lesson; it reports from the corner and puts itself away, since the cards are due now but answering them two seconds after reading the sentences they are cut from would teach nothing. **A reading that plants nothing now says why it planted nothing** (`llm/clozes.VerifyReport`, `readingNote`). It has several quite different causes — the model named no concepts, it wrote cards that one of `verify`'s rules refused, they were all already asked, or the database turned the rows down — and they want opposite fixes, but all four arrived at the reader as *this lesson is already asked every way it can be*, which the app had no way of knowing and which was false on the reading that prompted this. Three failures were silent: `verify` dropped each card without a word, a concept the database refused was skipped with its error discarded, and a refused batch of cards was reported as a concept with nought under it — indistinguishable, at the other end, from a lesson with nothing left to ask. A batch matters because one row failing `clozes_shape` takes the whole insert with it. The count of cards written, the reasons they were refused and the concepts lost whole for want of a single answerable card are now carried out of `verify` and said in the notice. The rule that most often turns a couple of refusals into a reading that plants nothing is `CARDS_PER_CONCEPT_MIN`: a concept left with none of its cards is dropped entire, so it is reported by name rather than merely counted. **A true-or-false is now written to a verdict drawn before the lesson is read** (`core/clozes.wantedVerdict`). Every one of them came out `False`, which was not luck: the brief asked for *a plausible confusion the lesson corrects*, and a confusion a lesson corrects is a statement that does not hold — the model did exactly as it was told and the shape was worthless, because a card whose answer can be given without reading it is graded *Easy*, honestly, and filed away for four months on the strength of a reflex. The draw is weighed against whatever statements the lesson already holds, so the word it is short of is the word it gets, and a coin where they are level or there are none; a coin rather than turn-and-turn-about, since an alternating deck has a tell in it. The brief says which word this reading is writing and how to write a statement that *holds* and is still worth judging — state the correct side of the confusion in the words the mistaken side would tempt — and tells the model to reach for a cloze or a question, never the other verdict, where a concept has nothing worth judging that way. `verify` then refuses a statement answered the other way and says so in the reading, because a preference that has already lost once is not what to hang the shape on a second time: a prompt is an instruction and a filter is a promise. Server-side, so the phone gets the mixed deck without shipping anything. |
| A card, stored as words rather than as an offset | built | planned (5) | `core/clozes.clozeFace`, `core/clozes.locateBlank`, `core/clozes.cardFront` | The same decision `020_highlights.sql` made for marks, for the same reason: a lesson body is written on demand and regenerable, so an offset into prose that has been rewritten points at nothing. A cloze holds the passage whole, the blank's words, and its offsets — both, because the offsets settle a word that appears twice in one sentence and the words survive an edit that moves the offsets. `clozeFace` is defensive about that pair on every read: offsets that no longer land fall back to the words, and a blank that is not in the passage at all gives a face with nothing hidden rather than a card that throws. The blank is drawn as a rule of fixed width, never the answer greyed out and never a gap the width of the answer — the length of a blank is a hint, and a hint nobody asked for. Since 046 the passage columns are nullable and a second shape shares the table: a standard card carries `question` and `answer` (and `note`, the one line a true-or-false is never written without) where a cloze carries its passage and offsets, and `clozes_shape` in the database refuses a row that is neither. `cardFront` and `cardBack` are how everything reads a card without branching on its kind; `cardAnchor` falls back to the passage for every cloze written before 046, when a card *was* its own quote, so nothing had to be backfilled for the wash to keep appearing where it did. |
| Tended passages drawn on the reading | built | planned (5) | `lib/paintPassages` (web), shared with `paintMarks`; `core/clozes.cardAnchor` | The sentence a card came out of wears a quiet plum rule in its own lesson, so re-reading says which of its sentences the garden is holding on to; pressing one opens that card, to be answered, rewritten or pulled up without leaving the reading. What is washed is the card's **anchor** (046), read through `cardAnchor`, which for a card written before then is the passage itself — the two were the same thing while a cloze had to quote its lesson, and they have come apart now that it does not. A card with no anchor is simply not drawn, exactly as a card whose lesson had been written again beneath it already was. Finding and wrapping is now one engine that the marks and the clozes share — they are the same problem, a passage stored as words, differing only in the wrapper and what pressing it does — and neither layer skips the other's wrappers, so a sentence that is both marked and tended is drawn twice in either order with the same result. The two carriers are deliberately different: a mark is a mustard wash *behind* the text from the baseline down, a cloze is a plum rule *under* it, so the overlap reads as washed and underscored rather than as a muddy third colour, and no override is needed that would make the appearance depend on which painter ran last. A press that ends a selection never opens a card — marking is select-and-let-go, and letting go inside a tended sentence must not take the selection over. On the phone the reader is the same WebView, so the engine goes with it. |
| Make a card by hand | built | planned (5) | `api/clozes.create`, `core/clozes.clozeProblem`, `core/clozes.cardProblem` | A selection offers *Add mark* and *Make a cloze* side by side, the same weight, and opens the panel on the one pressed: the same passage answers either question — what the reader thought about it, or whether they will still have it in a month — and neither is the default. Which is why **nothing opens on its own any more**. A mouse release used to open the note composer there and then; that was defensible while marking was the only thing a selection could do, and is not once it is one of two. It also means selecting a sentence merely to copy it no longer throws a panel at the reader. The two ways in still differ only in when the page may believe the selection is finished — a mouse says so by letting go, a finger only by stopping. The blank is chosen by pressing the words rather than typing underscores or dragging a second selection inside the first, which is not a gesture anyone can make on a phone; pressing two words apart takes everything between them, because a reader pressing *compound* and then *interest* wants the phrase. The card that will be asked is drawn under the words before it is planted. Planting closes the maker on the press and draws the plum immediately, under an unsaved id like a mark kept in the same session — drawing a tended passage needs only the passage and its prefix, so a draft is enough; the real row replaces it when it lands, and a failed write takes the plum back off rather than leaving a cloze that exists only on one screen. Until it lands the card can be answered but not edited or pulled up: there is nothing on the other end yet. Offsets are recomputed server-side and the prefix taken from the body, because the client's offsets are into the rendered prose and the painter searches the stored passage; the anchor a hand-made cloze sends is the selected sentence itself, checked like any other. The route takes all three kinds since 046 and judges every one through `cardProblem`, the same function the forms call, so a card the web refuses cannot be the card the phone writes — and a blank of more than five words is now among the refusals, by hand as well as by model, because that shape is the whole of what the overhaul was for. |
| The bench: work you can walk away from | built | planned (4) | `core/jobs`, `core/copy.LABOURS`/`OPENINGS`/`WRITINGS`/`TENDINGS`/`FILINGS` | Five kinds now: sowing, writing, opening, tending and **filing** — a diary entry being read back. `filing` is the one that names nothing, because an entry has no name: *Reading your entry back*, not *Reading Tuesday back*, which would be a notice about a stranger. Its failure line says the writing survived (*Your entry is kept, but could not be read back*), which is the one thing worth knowing when a model call falls over behind someone who has already closed the sheet. It leads back to the entry, where what the reading wrote is printed and any of it can be taken back. |
| Tend this lesson | built | planned (5) | `api/clozes.inLesson`, `core/clozes.shuffled`, `core/clozes.cardsPhrase` | The garden at the foot of the reading (046), between the prose and *How did you go?* — which is the order the two things happen in: you finish reading, you find out whether it stuck, and then you say honestly how you worked through it, with a card answered badly in hand as you do. One line of standing (*12 cards, 3 due*) and three things under it: a **sitting** over this lesson's cards, drawn shuffled from the deck already in hand rather than asked for again; the **deck listed**, to be read over, rewritten or pulled up; and **Write some more**, which adds and never replaces. The list is the one place in the app that prints a lesson's whole deck, and it can be a list where the Tend sheet cannot, because it is an inventory to fix rather than a queue to work — nobody feels behind for owning thirty cards, they feel behind for being shown thirty questions at once. Until this there was nowhere to find a card you could see was wrong except by waiting for it to come round, and a card the reader can see is wrong is a card they stop answering honestly. A row prints the front only, with a cloze's blank drawn as a rule: a deck listed with its answers showing teaches itself by being read. Pressing a row opens the whole card rather than a second set of controls that could come to disagree with the first about what pulling one up means. With nothing due, the sitting offers the whole deck anyway — a reader who has just finished the prose and wants to be asked about it is not asking the scheduler's permission. Folded away by default: a deck of thirty printed under every lesson is a backlog on a page they came to for the prose. |
| Tend link and its tally | built | planned (4) | `api/clozes.count`, `core/clozes.tendPhrase` | Between Marked and Inbox: what you kept, then what you are keeping hold of, then what is waiting to be filed. Stamped mustard on ink, like the inbox tally, and counting only what is **due** — the total is a fact about the garden, and a figure in a nav is a figure you are being asked to do something about. Held at module scope for the life of the tab, because the head is printed on every sheet and a number that vanishes and comes back on each navigation reads as a fault. Falls as cards are answered, through a window event rather than a navigation. |
| Tend the Garden: the four-hourly notice | built | planned (4) | `core/clozes.tendPhrase`, the bench's `offer` | A bench offer, not a toast of its own — the same slip of paper in the same corner as everything else the app has to say. Four hours is the design: a garden that asks every time you open a sheet is one you learn to dismiss without reading, and a notice nobody reads is worse than none, because it teaches the reader to ignore the corner where their work is also reported. Silent when nothing is due, and that silence does not spend the four hours — the next check still happens when there is finally something to say. Never on the Tend sheet itself, which is why it reads the address it is standing at, and therefore why it is behind a `<Suspense>` boundary in the root layout: a client hook reading URL data up there blocks the static shell of every route in the catalogue. It beats every ten minutes as well as on landing, because a browser tab lives for days and a four-hourly notice that only fires on a fresh load is a daily one. |
| A random card, from here | built | planned (5) | `api/clozes.random` | One line under the head of the stock list, a subject and a topic. The schedule says what is *worth* asking; a reader who wants to turn something over anyway should not have to wait for permission, and answering one still moves its schedule — it is a real answer, not a rehearsal. Scoped to what the reader is standing in: the whole site, one subject (resolved through `topic_subjects`), or one topic. Drawn in the application over a page of ids rather than with `order by random()`, which is a full sort on every press. The line prints nothing at all until something has been planted, so a new catalogue is not advertised at about a feature it has no use for yet. |
| Adjudicate pending topics | built | planned (5) | `api/topics.decide`, `core/adjudication`, `core/filing.filingClaims` | **Keeping one now files it.** `commit_ingestion` refuses to file a `pending` topic — it must not sit in a subject until the question about it is settled — and `confirm`, the thing that settles it, only flipped `state` to `active`. The deferred filing never happened, so every topic kept from the inbox landed in loose stock with its edges already drawn, which is most of how loose stock filled up. `confirm` now asks the bed on the same bar ingestion uses, and says how many subjects it filed under. Never fatal: the confirm stands either way, and an unfiled topic is on the loose sheet, which now says where it looks like it goes. Merge, split, keep — over two plates rather than two names. `core/adjudication` holds the sentences both platforms will print: how close in words, what a topic is holding, whether it is `established` (read at all — a filed resource is intent, only a lesson, mark or exposure says anyone has been there), and the one line of counsel for a given pair. **`043` fixes the merge itself.** `merge_topics` moved every edge onto the survivor and cleared the duplicates afterwards, but a unique constraint is checked per statement — so two topics both leading to a third, which is the ordinary shape of a duplicate and most of why they are being merged, failed the move with `edges_from_node_to_node_kind_key`. The clearing now happens first, keeping the stronger of the two weights. `043` also re-points `highlights`, `cloze_concepts`, `clozes` (all `set null`, so a merge cut every marked passage and card loose) and `highlight_tags` (`cascade`, so a merge deleted them). |
| The reading is asked for on its own | built | planned (5) | `api/subjects`, `api/subjects.resow` | The reading was an optional field on the map's tool, and a bed laid out from a document to the letter crowds it out: "record exactly one topic for each of the entries above, in the same order, and no others" is a strong instruction, and beside it an optional field is one the model skips. A sheet with eight answered questions stored `assessment: null` and the reading printed "sown without answering anything" — the one thing it must never say to someone who has just answered. Wording it harder moved it from never to one time in five; making it `required` was worse, and the model began handing the whole tool input back as a JSON string under `topics`, failing the sowing outright. The two jobs are not one job: `readTheAnswers` asks for the reading in a call of its own (~6s, five times in five), used where the map left it out. Skipped when there is nothing to read or under 12s left — the planting is what a subject cannot be sown without, and a failed reading leaves the bed standing. |
| Add a topic by name, placed in the bed it was added to | built | planned (5) | `api/subjects.topics` | Two readings: the resolver against the whole map, then an LLM sort against this bed, which draws the edges that put it under something. The sort may raise an adjudication and may never settle one. |
| Move a topic between subjects, or file it under several | built | planned (5) | `api/subjects.fileTopic`, `api/subjects.removeTopic`, `api/topics.patch` | A topic belongs to every subject it genuinely sits under — exposure to portrait *and* landscape photography, JavaScript to front-end *and* app development — and `012` made it able to say so. Nothing could say it after the fact. `POST /api/subjects/[id]/topics` takes a `topicId` as well as a title: no resolution, because the caller holds the row rather than a name, but the same sort into the bed, so a topic arriving from elsewhere is placed rather than left floating. A move is file-then-unfile, in that order, so a failure leaves it filed somewhere rather than nowhere. The home subject (`primary_subject_id`, the ink the graph draws it in) is set from the same block. |
| Promote a topic to a subject | built | planned (5) | `api/topics.promote`, `044` | Ingestion files what it reads at one level and cannot know which of those is a bed you will spend a year in. The topic and everything the outline hangs under it move into the new subject and take it as their home. **The topic row survives** — `exposures.topic_id` is not null and a subject is not something you can have read, so consuming it would destroy the reading log; the name appears twice until the reader folds or renames it, and the sheet says so before the press. Additive and reversible. |
| Demote a topic into a route, as a lesson | built | planned (5) | `api/topics.demote`, `044` | The mirror, and the destructive one: everything the topic holds moves onto the target first and the row is then deleted, so the only loss is the name — which becomes the lesson's title. What it was read from becomes the lesson's `lesson_resources`. Two presses, and the second is only offered once the sheet can say what the first would move, the same shape as grubbing out a bed. |
| A topic carrying a route cannot change level | built | planned (5) | `044.topic_has_a_route` | The gate on both of the above, **in the database rather than in the route**, because it is the invariant and not one caller's policy. A curriculum is a plan someone approved and is working, with lessons written, opened and completed against it; rehoming that is a different and much larger operation. It is also what makes `044` small enough to be safe: no lessons to re-parent, no prereq graph to rewrite, no approved plan to invalidate. The sheets say why rather than hiding the control — a missing control reads as a fault.
| Sow: qualifying questions answered during the form | built | planned (5) | `api/subjects.qualify` | |
| Draft, reshape, approve a curriculum | built | planned (5) | `api/curricula`, `core/curriculum` | |
| A course carries the reasoning behind it | built | planned (5) | `core/learningPlan`, `api/curricula.plan`, `049` | **The reasoning existed once and was thrown away.** `curricula` records what a course is -- its title, its shape, its lessons and their order -- and nothing about why it is that. The argument was made inside the call that drafted it and died with the tool result, so every agent that touched the course afterwards worked from the artefact alone: a lesson body written against a title and its neighbours, a deck cut from a body, none of them able to see that the route opens shallow *because* the qualifying answers showed the reader already holds the fundamentals, or that the middle is thin on purpose because a handed-over source covers it. Each one then re-derived an answer to that, differently, and the course drifted away from itself a lesson at a time. `049` `curriculum_plans` holds three things: the `reasoning` the drafting agent now writes as part of its own tool call, a **snapshot** of the qualifying answers from `subject_sowings` (017) -- snapshot rather than a read-through, because sowing the same subject again would otherwise silently re-read a year-old course as though it had always been planned for the person the reader is now -- and `entries`, an append log. `core/learningPlan.planPrompt` renders all three into the **system prompt** of every agent contributing to the course, beside `llm/voice.LESSON_VOICE` and for the same reason: it is true of every round of every lesson, so it stands in front of the cached prefix rather than inside the request. It returns null for a course with nothing to say, because a heading with nothing under it teaches a model that the section is noise. A lesson agent appends one line when its body finishes -- what the lesson *actually* taught rather than what it was asked to, which is the part nobody else can infer -- written by a separate Haiku call (`llm/plan.ts`) rather than as a marker in the streamed prose, since a marker the stripping missed would print to the reader as part of their lesson. The append goes through `append_plan_entry` rather than a read-modify-write: two lesson agents finishing at once would each read the log, add to what they read, and write back, and the second write would drop the first agent's line with nothing noticing. The reader owns the `reasoning` outright and may rewrite it; the log is a record of what was written and is never rewritten, so disagreeing with a line means adding one beside it, marked as theirs. Nothing on the map reads from the plan, so it invalidates nothing. **The sheet is not built on either platform** -- the document, its agents and its route exist; the surface that prints and edits it does not. |
| A sown bed arrives with somewhere to start | built | planned (5) | `api/curricula.draftAndOpen`, `api/lessons.writeWhole`, `core/jobs` (`opening`), `core/copy.OPENINGS` | A subject used to arrive as twenty topics and nothing to read, with three decisions between the reader and a first page — which topic, what to ask a route for, whether to write the lesson — whose commonest answer to all three is "start at the beginning". So the app answers them itself, once, for the first topic only: the rest of the bed is untouched until someone asks. The map is now asked for its topics *in the order they should be met* and that order is kept (`topic_subjects.position`), so "the first topic" means the most introductory one rather than the first alphabetically; anything `pending` is skipped, because a topic that may be about to be merged is not somewhere to write lessons against. A route the topic already carries is followed instead of a second being drafted — topics are shared across subjects, so the most introductory thing in a new bed can be something already worked elsewhere. Nothing is approved: the route lands as a draft exactly as a hand-drafted one does, and what this removes is the blank page, not the decision. It runs as its own bench job so the sowing still reports the moment the bed lands rather than waiting two minutes for a lesson, and so the reader may walk off — it cannot survive a reload, and what is left behind then is a drafted route with one unwritten lesson, which is a sheet with a button on it. |
| Write a lesson body; write it again | built | planned (5) | `api/lessons.writeBody`, `api/lessons.writeWhole`, `core/jobs` | Written in rounds: a full lesson takes longer to generate than a function is allowed to run, so each round is its own request and is saved, and `body_finished` (028) is what keeps a half-written lesson from printing as a whole one. The client drives the loop from the bench, so it survives navigation and reports the round and word count as it goes; six rounds is the cap. The loop itself is `lessons.writeWhole` in `@didactic/api`, because three callers set the same work going — the topic sheet's control, the lesson's own first open, and the opening of a freshly sown bed — and a loop written three times is a cap enforced three times and a progress note (`core/copy.roundPhrase`) worded three times. Resumable — a reader who reloads mid-way is carried on from the prose on the row. Continuation is a user turn, never an assistant prefill, which every current model refuses with a 400. The round's ceiling buys prose alone (`llm/thinking.NO_THINKING`): `claude-sonnet-5` thinks unless it is told not to and spends `max_tokens` doing it, so a round that never said so came back with a thinking block and no lesson in it — which is the same class of fault as the prefill above, the model's defaults moving under a ceiling measured against the answer. The two shelves the prose may point at are bounded for the same reason: the near one was every resource ever filed against the topic, which is a prompt that grows with the library rather than with the lesson, and both now take the forty best by `resource_topics.relevance` rather than the forty Postgres happens to return. **How it is written is now stated, and stated apart from what it is about** (`llm/voice.LESSON_VOICE`): every prompt in that directory handed the model facts — this topic, this shelf, this map — and none of them said a word about the prose, so what came back read like every other model-written article on the internet. An opening paragraph announcing the subject, lists of three by reflex, *it's crucial to understand that*, a closing line telling the reader they are now well equipped. The voice asks for a practitioner explaining something to a smart friend: a concrete thing attached to every abstract claim, the structure the topic needs rather than a template, an opening that is the first useful thing and an ending that is where the content ends, and a named list of the words that are tells. It is the `system` prompt rather than another paragraph of the request, for three reasons: it is the same on every call, it is about the writing rather than the subject, and it therefore stands in front of a carried round — where an instruction buried in the opening user turn is furthest away and least likely to be followed. The refresher is written in the same voice, being the same prose read by the same person on the same kind of sheet. |
| Complete a lesson at a depth | built | planned (5) | `api/lessons.patch` | |
| A right answer boosts the topic's figure | built | planned (5) | `core/answers`, `core/config.DEPTH_WEIGHTS`, `api/lessons.answer` | Reverses the earlier decision not to score questions, and answers its reasoning rather than dropping it: only the first answer to a question counts (a unique index in 027, not a read), a wrong answer subtracts nothing, and the `answered` weight sits just above `marked` and far below a skim. What it was worth is printed under the question — scoring quietly is the version that was worth avoiding. A question is keyed by a hash of its own text, so a rewritten question reads as unanswered rather than inheriting an answer. A scaffolding lesson moves nothing, exactly as completing one does not. |
| Lesson blocks: chart, model, check, blank, sort, compare, steps, flow, picture | built (SVG + DOM) | planned (5) | `reader`, `core/blocks`, `core/answers`, `core/pictures`, `core/flowGraph` | The same components inside the reader; every plot ships its figures on both. `blank` asks for recall where `check` asks for recognition; `sort` asks the question a multiple choice cannot — a rule with several instances under it, and **since 050 it is a board**: the groups are columns, the items are cards, and a card moves by an arrow that names the column it is going to rather than by a drag, which is awkward on a phone and unusable with a screen reader. It was a list of rows carrying group buttons, which showed the reader everything except their own answer — and every class it named was missing from the stylesheet, so it had been printing as a bulleted list of browser-default buttons in the middle of a catalogue. A checked board keeps the cards where the reader put them and each wrong one says where it belonged. Columns stack on a phone rather than scrolling: a board is worked, not looked at. Both grade in `core/answers`, so the phone marks them identically. A payload whose gaps and answers do not agree, or whose items name a group that does not exist, stands down rather than printing a question nobody can get right. `model` is the one block that computes rather than prints: sliders drive formulas read by `core/expression`, a closed arithmetic grammar with no property access, no calls but a whitelist and no globals, so the payload stays data. It draws through the same `Chart`. `picture` addresses are **resolved when the lesson is written** rather than trusted: a model cannot check a link, and a Wikimedia upload path is the one address it could never get right — the path carries the first characters of the MD5 of the file name, so the shard is a two-hex-digit guess on top of the name. The model is now asked for the Commons file page, `core/pictures` pulls the name back out of whatever form was written (upload path, thumbnail, file page, or the `#/media/File:` fragment a person would paste), and the web asks Commons where it actually lives, trying the other extensions too since a diagram remembered as a PNG is often the SVG it was rendered from. A picture that cannot be found is taken out of the body and the writer is told. Nothing of this is a read path, so the phone inherits bodies whose pictures already resolve; only `core/pictures` is shared, because the phone parses the same blocks. **A first answer is no longer reported as a repeat** (`blocks/answering.useQuestion`): the channel closes a question the moment it is pressed rather than when the server answers, deliberately, so that a second press while the reader is reading the explanation cannot pay twice — and `already` was read live at render, so the block's own answer arriving in the channel was back in time to tell the reader they had answered it before. Every first answer to every question was met with *it counts for nothing now*, the opposite of what had just happened. The record was never touched: the unique index on (user, lesson, question) is what actually decides whether an answer counts, and it decided correctly throughout, which is why this survived — the figures were right and only the reader was misinformed. `already` is latched at the press now, so the wording is about the state of the world when the reader answered; a second answer in the same sitting reads correctly and sends nothing. |
| Contents band | built | planned (5) | `reader`, `core/sections` | One column on the phone. Travelling to a section writes the hash without a navigation; see `lib/hash.ts`. |
| Lesson links out to other lessons | built | planned (5) | `reader`, `core/lessonLinks` | Named `lesson:<slug>` in the body, resolved when it is read against the topic and the topics its subjects hold. |
| The way on at the foot of a lesson | built | planned (5) | `core/lessonState.lessonNeighbours` | Previous on the left, next on the right, each naming the lesson it goes to. Past the reading and past saying how it went, which is the order the two happen in. Neither side is gated on completion — nothing here is locked, and a lesson that builds on uncovered ground says so at its own top instead. `neighbours` on `lessons.get`, derived from the route's position order. A native stack header gives the phone the back half; the forward half is the sheet's own. Each neighbour says whether it is written, which is what lets the half-way offer know there is anything to offer. |
| A named lesson that does not exist prints as a stub | built | planned (5) | `core/lessonLinks` | Faint ink and a dotted rule, no href, `title` saying so. Colour is not the carrier. |
| Lessons cite the reader's own documents | built | planned (5) | `core/sourceLinks`, `reader` | Named `source:<slug>#p<page>` in the body and resolved when it is read, exactly as `lesson:` is and for the same reason. The writing agent is handed the six passages nearest to what the lesson is about, with their pages, and told to cite those and nothing else — and that citing nothing is a legitimate answer, without which it reliably finds a reason to cite all six. The page rides in the href's fragment so a citation is a whole address: followable, middle-clickable, copyable. Plum with a superscript mark, so a citation does not read as another lesson to go and read. |
| A citation that cannot be checked prints as a stub | built | planned (5) | `core/sourceLinks.resolveSource` | Two cases, both meaning the reader cannot verify it: a document that has left the shelf, and a page past the end of one. The second is the one that matters — a citation of a real book at a page nobody handed over reads exactly like a real one, and the reader is not going to check. The body is also read back against what it was actually shown (`citations.unsupportedCitations`) and anything unsupported is reported with the round rather than trusted. |
| Marks: draw kept passages onto the prose | built (DOM walk) | planned (5) | `reader/paintMarks` | The same walker, in the WebView. Removing a mark takes the wash off the words on the press, from the panel and the list alike: both go through one removal that holds the id in `gone` until the sheet has caught up, or the next repaint draws the removed mark straight back. |
| Marks: select any range and keep it, across elements | built | planned (5) | `reader/Highlighter` | Full parity through the reader (D9). The per-paragraph native fallback would be `partial` and is not the plan. |
| Marks: a note on the lesson with no passage | built | planned (5) | — | *A note on this lesson* wording shared. |
| Marks: name a topic or a lesson in a note with `@` | built | planned (5) | `core/mentions`, `core/mentionSearch` | Suggestions as you type, from `api/mentions`. What is chosen is written in as a link to that thing's own address, so it needs no scheme and survives the editor's round trip. |
| Write an entry from anywhere | built | planned (5) | `api/diary.create`, `core/jobs` | The one control on every sheet that is not a way to another sheet, so it is printed into the masthead band with the sheets and set apart from them by a rule — the same way leaving is. Not a floating button: this world has no chrome that is not printed on the sheet, and no rounded corners for one to wear. What it opens is not a modal either — the composer unrolls under the band on the sheet's own paper, at the sheet's own measure, and the page carries on existing underneath with nothing dimmed and nothing trapped. Escape rolls it back up. An entry opened from a topic or lesson sheet starts filed under that topic and the composer stamps *Filed under* with its name — a tag nobody can see is a tag nobody can correct. The name is passed in by the sheet rather than parsed off the address: only the sheet knows what the thing it is printing is called, and this is also what removed the Suspense boundary the first version needed. The box is half again the height of a note box, because a note is a remark about a sentence and an entry is a page about a week. The entry saves on the press; the reading of it goes to the bench. |
| Sow a new subject, from the subjects sheet | built | planned (5) | — | Where the garden line used to be. Two quiet links to the tending sheet stood beside this title on the one sheet that already carries Tend in its running head with the due count stamped on it — so the sheet asked three times for the same thing and never once for the thing offered nowhere else. Ruled rather than filled: a plate-green fill would be the loudest thing on a sheet whose subject rows are the point, so it takes the green as an outline and fills on hover. The garden line stays on the subject and topic sheets, where it is scoped to what you are looking at. |
| Write a diary entry | built | planned (5) | `api/diary.create`, `core/mentions` | A page about a week rather than a remark about a passage: what is sticking, what is not, what got shipped at work. The same editor and the same `@` tagging a note uses, and the same row — a highlight of kind `diary`, with no quote and no lesson. Where the reader was standing is kept as a starting point and not a claim; what the entry is about is what it names, so pressing the button on a topic sheet and then writing about something else means deleting a mention rather than fighting a form. Writes no exposure: nothing has read it yet, and guessing a depth from the fact that someone typed would be exactly the flattery the map exists to avoid. |
| Read an entry back for what it shows | built | planned (5) | `api/diary.read` | A model call over prose, so it is its own request behind the bench rather than part of saving. Five verdicts per topic the entry *names*, **and the topic it was written from** — the model never chooses the topics, the reader's own `@` tags and the sheet they wrote on do, and anything it answers about that is not in that list is dropped. The written-from topic was missing: an entry written on a topic's sheet about that topic rarely names it, so it was read against nothing and never reached the figure beside it. The prompt says the entry may be about that topic without naming it, and holds it to the same rules. `applied` (they built or shipped it), `read`, `skim`, `struggled` (they say it is not landing), and **`mentioned`, which records nothing at all**: a topic named only as context for something else is the common case, and a model with four options and no way to say "no claim" would have had to pick one — the cheapest way to get a wrong `read`. The exposure's reason is the writer's own quoted words, so the topic sheet explains its figure without anyone opening the diary. Re-reading replaces the last reading rather than stacking a second opinion. |
| Take back what a reading claimed | built | planned (5) | `api/diary.revoke` | The whole reason the reading is allowed to run unattended. The app read someone's prose and moved a figure they cannot edit by hand; the least it can do is let them say no, at any time, without explaining themselves. Deleting the exposure is the whole of it — ability is a cache over the log, so the claim goes when the row does — and it is scoped to `source = 'diary'`, so it can never reach an exposure written by reading a lesson or tending a card. |
| Marks on the bed, with what they are about | built | planned (6) | `core/graphMarks` | A layer like material and lessons, off until asked for. Two kinds of line: faint to the topic it was marked in, stronger to everything the note names. Newest 500. |
| Marks: the list beside the reading, in the lesson's order | built | planned (5) | `reader/MarkList`, `core/marks.inReadingOrder` | `DESIGN.md` *The marks beside the reading*. Sorted by where `paintMarks` landed each mark, never by when it was kept. Pressing a passage travels to it. On a phone it is over the reading and puts itself away first; the swipe that opens it is the WebView's to carry (D9). |
| Note editor with bold, italic, lists, headings, quotes, specimens | built (`execCommand`) | planned (5) | `reader/NoteEditor` | The same editor, inside the reader; the soft keyboard is the native side's to handle. Headings, quote and code block were added for the learning diary: an entry is a page about a week, not a remark about a passage, so `NOTE_TAGS` widened to `h1`–`h6`, `pre`, `blockquote` and `hr` — one allowlist for both, because the difference between a note and an entry is where it was written rather than what it may say. Still no tables: nothing writes one by hand in a box like this and the serialiser has no way back from one. A block control toggles off when pressed in the block it made; the fence is built by hand because `formatBlock` gives a `<pre>` with no `<code>` in it. LaTeX needed nothing — `lib/maths.ts` already typeset `$…$` through KaTeX to MathML and `MATHML_TAGS` was already in `NOTE_TAGS`. |
| Refresher generation | built | planned (5) | `api/refresher.write` | Written in the same voice as a lesson body (`llm/voice.LESSON_VOICE` as the `system` prompt): the same prose, read by the same person, on the same kind of sheet. |
| Grub out a subject or topic; delete flows | built | planned (5) | `core/adjudication.grubbingOut`, `core/loose.reckon` | **A topic could not be deleted from either sheet that shows one.** The row has said *built* for as long as the subject's grub-out has existed, and for a subject it was true; for a topic the only ways to destroy one were the graph canvas's topic panel and the loose sheet's bulk throw-away — neither of which is where anyone looks for a topic, and the second of which only ever lists topics filed nowhere. The bed, meanwhile, offered *Remove* against every row in edit mode, which reads as a delete and is not one: it unfiles the topic and leaves it standing as loose stock. So the two sheets that print a topic offered either nothing or something that looked like the thing it was not. The bed now words them apart — *Take out* unfiles, *Grub out* destroys and asks a second time against the row it names — and the topic's own sheet carries the full block at its foot, past everything the topic holds, the same shape and furniture the subject sheet grubs a bed out with. **What the warning says is the point, and it is not uniform.** A topic takes its curricula by cascade (014), a curriculum takes its lessons, and a lesson takes the concepts and cards cut from it (035) — so a route and a deck go without ever being named by a count of the topic's own rows, and the reader would have no way to know. A mark is the deliberate opposite (022): the reader wrote it, so it outlives the topic and stays on the Marked sheet filed under nothing, and the material stays in the Library. Both halves are printed, because the anxiety of this press is never *will it go* but *does my reading go with it*, and the half that reassures is the half nobody can infer from the schema. `grubbingOut` is in core rather than in either sheet for the ordinary reason: three surfaces ask it and a warning that counted differently in two places would be two accounts of one loss. `/api/topics/[id]` gained an additive `marks` count so the confirmation can state the surviving half — counted, never listed, since the only caller is a sentence. |
| Deep links to any address | built (they are URLs) | planned (4) | the address table in `ARCHITECTURE.md` §6 | Universal links on the web origin. |
| Realtime: tally and inbox refresh when an ingestion lands | n/a (the web refetches on navigation) | planned (5) | `api/endpoints` tags | Scoped by RLS; a signal to refetch, never a source of truth. |
| Direct row reads from Supabase | n/a | none yet | — | Now enforced by RLS rather than only permitted by it. Allowed per row with a reason; the API is the default. Add a row per read taken. |
| Galley loading states | built | planned (4) | — | Same three shapes: prose, rows, panel. |
| Motion: sheet and band arrive, feedback on controls | built | planned (4) | `tokens.motion`, `tokens.ease` | Reanimated on the phone; no reduced-motion guard on either, by decision. |
| Scroll memory across navigation | built | planned (5) | — | Native stacks keep it by construction. |
| Variable-font axes SOFT / WONK / opsz | built | partial | `reader` fonts | Available inside the reader (a browser engine), so the lesson body has them; the native sheets use static optical-size cuts. See `guides/styling-on-mobile.md`. |
| Paper tooth texture | built | planned (4) | — | Tiled image or Skia shader; must read as threads, not noise. |
| Dark mode | built | planned (5) | `tokens.colourDark`, `tokens.plateDark`, `tokens.graphDark`, `core/graph.PAPER_DARK`, `core/lighting`, `components/useTheme` | **This row said `n/a — by construction` and the construction was wrong.** The rule it pointed at held that a catalogue sheet has one side and one lighting condition, which is true of the object and false of the reader: this catalogue is read in the evening, and a cream sheet at full brightness in a dark room is not a faithful printed object but a lamp. Colour is the whole of the difference — the scale, the spacing, the measures and the timing are the same object under either light, and the dark block restates nothing but inks. Three states rather than two: an explicit choice stamps `data-theme` on the root, the default stamps nothing and follows `prefers-color-scheme`, and the media query is guarded against an explicit light rather than left bare. The choice sits at the foot of the subjects sheet beside the way out, and an inline script in the head stamps it before the first paint, since a correction made after mount is a white flash in a dark room. **The five plates are relit, not inverted**: five of six are unreadable as text on the dark ground, plum worst at 1.9:1, so each is raised in lightness and eased in saturation until it clears 4.5:1 with its hue held — mustard alone is unchanged, having always been lit for a dark ground. That does mean a subject is a slightly different green after dark, which is the real cost, and the alternative is a bed nobody can read. **The masthead is the one surface that does not invert**, staying a deep plate with bone reversed out of it under both lights, because a band that flipped to a pale fill with dark text would stop reading as printed and start reading as a panel; terracotta parts company with it and stays bright, being the one interactive accent, so what reverses out of *it* flips instead (`--on-plate` / `--on-accent`). **The bed is the exception that proves the rule.** Everything else follows a custom property; the graph canvas paints to a bitmap no stylesheet reaches, so it is handed its inks and its ground. `fade()` mixes every seed toward that ground as a topic goes dormant, and a dark bed fading toward paper would print the cold topics as the brightest things on the map — the one surface whose whole job is saying what has gone cold, saying the exact opposite. `core/tests/graph-ground.test.ts` holds that both ways round. **The choice is now drawn, and it stands at the foot of every sheet rather than one** (`core/lighting`, `components/SheetFoot`). Three words at the foot of the subjects sheet is a setting you have to already know about to find, put on the one sheet nobody is standing on at the hour it matters: the room goes dark while the reader is in a lesson or in the garden, not on the catalogue front. It also put a setting about the *room* on the *paper*. It now sits in the bottom trim of every sheet, on the sheet's own paper, rendered from `layout.tsx` outside `main`, so no sheet carries a copy and fifteen of them gain a foot they never had. It stood on the press bed *below* the trim first, and a ground a shade darker than the paper turned three glyphs into a banner section — furniture bolted under the page rather than its last line. The catalogue runs to six sheet measures and every one ends with the same `var(--space-6)` of clear paper, so the foot takes that margin back and needs no ground, no tooth and no width of its own to keep in step with six others; centred, because the middle is the one position that lands on paper whatever the measure. It is lifted a single z-index to be visible at all, `sheetIn`'s transform making each sheet a stacking context that paints over anything overlapping it from normal flow. The one in use is inked and ruled under in mustard like every other action at a foot, a struck disc behind the glyph having been the loudest thing on the sheet. The three forms are one disc under three conditions: a disc with rays, a disc bitten to a crescent, and a disc half lit for *ask the room*, the half-lit one drawn at the crescent's radius because the sun carries rays the other two do not. Geometry in `core/lighting` with `core/tests/lighting.test.ts` behind it, so the phone draws the same three with `react-native-svg` off the same paths and the setting cannot wear a different face on each platform; the words it used to print are there too, as each glyph's accessible name, since a control whose whole face is a drawing has no visible text to fall back on. **The bed map is the one route without it**: `/graph` is a canvas the exact height of the window and panned by dragging, so a strip below it makes a page that scrolls under a drag that overshoots. Getting here first required **collapsing 130 inlined `rgba()` copies of ink and paper into alpha tokens**: the light theme's ground was stated in 130 places no dark block could reach, and there are now no colour literals outside `:root` at all. The agreement test reads the two blocks separately and fails if either drifts, or if a colour the light theme states is left out of the dark one — which is how a dark sheet ends up with one cream rule across it, visible to anyone looking and invisible to every test that only reads the block it was added to. |
| Reduced-motion guard | n/a | n/a | — | By decision, `DESIGN.md` §7. |

## Backend

| Item | Status | Notes |
| --- | --- | --- |
| Supabase project, migrations, edge functions | shared, unchanged | `supabase/` stays at the repo root. |
| API routes as the backend for both | built | `guides/api-contract.md`. Cookie or bearer, both verified in `getOwner()`. |
| Row-level security on every table | built; applied per project | `024_row_level_security.sql`: all 18 tables under `public`, owner policy on the nine carrying `user_id` (`highlights` kept its own from 020), join tables through their parent. Storage's `resources` bucket joins back through `resources.storage_path`, because upload paths carry no owner id. Service role unaffected. Every step is idempotent, because a project that was already live when this was written takes it by hand: a migration in the tree is not a migration on a database, and this one was found missing on the remote project while the local stack had it. Check with a publishable-key `select` on `topics` — it must return nothing. |
| Documents as passages, outline and rounds | built | `029` `resource_passages` (384-dim, matching gte-small and the rest of the map, with an hnsw cosine index and a partial index on the ones still without a vector) and `match_passages`, which takes the set of documents the caller is allowed to cite rather than working it out. `030` `resource_outline` and the cursor on `ingestion_jobs` — `pages_done`, `page_count`, `ms_per_page`. `031` `source_fidelity` on the two joins that already carried a handed-over document, `resource_subjects` (023) and `curriculum_sources` (014). Owner policies in the shape 025 set; both new tables carry `user_id` directly rather than reaching through the resource, because every query here filters on the owner and a join per passage is a join too many. All idempotent: Supabase applies them on the push to `main`. |
| pdfjs finds its own worker | built | pdfjs parses in a worker, and in Node runs it in-process by importing `pdf.worker.mjs` from a path it builds at runtime — invisible to file tracing, so the file was never deployed and `getDocument` failed with "Setting up fake worker failed", which reached the sheet as a document that could not be opened. Two halves again: `next.config.ts` names the file in `outputFileTracingIncludes` for the two routes that open a document (and only those — it is two megabytes and has no business in the function that draws a graph), and `extract/pdf.ts` resolves it with `require.resolve`, a literal specifier a tracer can see, then sets `GlobalWorkerOptions.workerSrc` to its file URL. `pdf-parse` gets the same through `setWorker`, because it calls `getDocument` itself and the one line that would set a worker is commented out in its source. Note the guard is *not* "if unset": pdfjs ships with `workerSrc` already set to the relative `./pdf.worker.mjs` it means to resolve against itself, which is the resolution that fails, so an emptiness check fires never. Verified in the build's trace files: the worker is listed for `/api/resources/uploaded` and `/api/internal/ingest`, and absent from `/api/graph`. |
| pdfjs reads PDFs without a canvas | built | Two halves, because the first alone was not enough. **Bundling:** `next.config.ts` `serverExternalPackages` names `pdf-parse` and `pdfjs-dist`, which Next does not externalise by itself. Inlined into a server chunk, pdfjs's runtime `require` of its canvas resolves against the chunk and fails. Verified by building both ways: without it the output carries `node_modules_pdfjs-dist_legacy_build_pdf_mjs_*.js`, with it no pdfjs source at all. **The global:** unbundled, pdfjs sits in `node_modules` and *still* cannot find `@napi-rs/canvas` — nothing statically imports it, so file tracing has nothing to follow and the native binary never reaches the function; declaring it as a dependency does not change that. Node has no `DOMMatrix`, and pdfjs builds one at module scope (`const SCALE_MATRIX = new DOMMatrix()`), so it throws `ReferenceError: DOMMatrix is not defined` during evaluation, where no `try`/`catch` at a call site can reach it. `lib/extract/pdfGlobals.ts` supplies a stub as the first import of `extract/pdf.ts`, so the native package is not needed anywhere. Its methods throw rather than returning something plausible: every path that would use one is a rendering path, and nothing here renders. `ImageData` and `Path2D` are left undefined for the same reason — glyph paths and clips only. The stub is installed before pdfjs looks, so it wins even in development, which means `tests/document.test.ts` exercises the exact arrangement the deploy runs in rather than a luckier one. |
| Realtime publication for `resources` and `topics` | built | For the phone's tally and inbox. A publication is not a grant: RLS still decides what a subscriber sees. |
| The order a bed was sown in | built | `033_bed_order.sql`: `position` on `topic_subjects`, nullable, with the order riding on the membership rather than on the topic — a topic sits under every subject it genuinely belongs to, and "third thing to meet" is true of it in one bed and not in another. Exposure is introductory in photography and advanced in a course on sensor physics; one column on `topics` could only ever say one of those. Null is a real answer and needs no name: a topic added by hand afterwards, or a bed sown before this existed, which the outline sorts last rather than guessing at nought. Idempotent, as everything here must be. |
| The learning diary, and struggle as evidence | built (schema and scoring) | `040_diary.sql` adds `kind` to `highlights` (`'mark'` or `'diary'`, defaulted to `'mark'` because that is what every existing row genuinely is) plus a check that an entry carries no quote and no lesson — enforced in the database rather than the route, since the phone writes under the owner's token with no handler in front of it — and an owner/recency index, which is the one the timeline reads on. An entry is a highlight rather than a table of its own: `highlights` already allowed a null quote (020) and a null lesson (022), and `highlight_tags` (024) already indexes what a note names, so a second table would have duplicated the mention parser, the search and the tag index and then needed a union to put them back into one timeline. `041` adds the two enum values alone, as 013 established: Postgres forbids *using* a new enum value in the transaction that added it, and the runner wraps each file in one. **`struggled` is a depth weighted 0, and that alone was not enough.** A negative `ability_delta` does nothing — the column is written by every exposure and read by nothing, since `computeAbility` derives ability from `depth` alone. But weight 0 on its own made the app *more* sure, not less: a struggle is one more exposure of one more kind, so both terms of the confidence formula rose. The test written to prove the intended behaviour proved the reverse. So the depth does half the job and a clamp does the other half — a struggle that is the most recent real exposure holds confidence at `STRUGGLING_CONFIDENCE` (0.35), under the `CONFIDENT_ENOUGH` (0.4) line where all five surfaces print a figure as vague. Ability is never touched: a month of genuine reading survives a bad week, which is what rules out the subtract-from-weight design. It clears itself in date order — read, skim or apply the thing and the struggle is history, with no *mark as resolved* anywhere; a marked passage does not clear it, because keeping a sentence is evidence you were there rather than evidence you have got it. `'diary'` joins `exposure_source`, and `applied` (weight 1.0, the only depth that passes the 3.5 ceiling) finally has something that writes it. |
| The garden: concepts, clozes and their reviews | built | `034_clozes.sql`: `cloze_concepts`, `clozes` and `cloze_reviews`, with the `cloze_state` and `cloze_rating` enums. What FSRS computed lives on the cloze row (`stability`, `difficulty`, `state`, `reps`, `lapses`, `due`, `last_reviewed_at`) and the arithmetic deliberately does not live in the database: it is fitted, it will be refitted, and both front ends have to agree about it, so it sits in `packages/core/src/fsrs.ts` with its tests and the database holds only the answer. `stability` and `difficulty` are nullable rather than zeroed — a new card has no memory to describe, and a zero is a lie the scheduler would then reason from. `due` defaults to `now()`, so a cloze is due the moment it exists and the lesson just worked can be tended the same day. A check constraint keeps the blank inside its own passage, because a card whose blank runs off the end of its text is unanswerable and the front ends would otherwise have to guard every read. `topic_id` is denormalised off the lesson's curriculum exactly as `highlights` denormalises it (020): tending is asked for by topic and by subject far more often than by lesson. `cloze_reviews` is the log, kept apart from the row for the reason the exposure log is kept apart from the ability figure — the row says where a card stands now, the log is the only thing a refitting could ever be run against, and it records `retrievability` at answer time, which is the one number that says whether the schedule is any good. Owner policies in the shape 025 set; idempotent throughout, since Supabase applies it on the push to `main`. **`035` tightens three foreign keys from `set null` to `cascade`**, after checking the chain against Postgres rather than reasoning about it. `clozes.concept_id` was a live bug: reading a lesson again deletes its concepts and writes a new set, and the code said the cards went with them — they did not, so the one path that exists to *replace* a lesson's cards doubled them instead, and did it again on every press. A cloze the reader made by hand belongs to no concept and survives a regeneration, which is the other half of what that code meant. `topic_id` on both tables was latent rather than live: grubbing out a topic already took the clozes through curricula → lessons → clozes, and grubbing out a subject takes the topics no other subject holds and so takes them too — but `set null` *stated* that a cloze outlives its topic, filed under nothing, which is the opposite of what is wanted and only unreachable because every cloze carries the topic of the curriculum its lesson sits in. A mark is the deliberate opposite (022): the reader wrote it, so it outlives both. `tests/clozes-cascade.integration.test.ts` holds all of it against a real database. |
| Read endpoints for server-rendered sheets | built | `/api/home`, `/api/inbox`, `/api/graph`, `…/area`, `…/sowing`. `/api/library` is gone with its sheet; see the deprecations table in the API contract. |
| Server cache held until invalidated | built | The six `use cache` readers in `src/lib` run the `held` profile from `next.config.ts`: a year's `revalidate`, no expiry, so only `revalidateTag` re-reads the database. Safe because every mutating route drops its tags, which `tests/cache-invalidation.test.ts` enforces both ways — a write that drops nothing fails, and so does a cached reader that states no lifetime. `stale` stays at five minutes: it governs the browser's own router cache, which no tag can reach. The phone's query cache should hold on the same terms. |
| Typed API client (`@didactic/api`) | built | One function per route, `Result<T>` from `readJson`, never throws on an HTTP error. `ENDPOINTS` names the tags each write drops, read by the server cache and the phone's query cache both. `mentions.search` is the one read asked on a keystroke; it drops a stale answer rather than aborting, because the client takes no signal and a menu only needs the newest. |
| Scoring maths in the Deno edge functions | none duplicated | Checked at 2.8: neither function holds any. `embed` runs gte-small and answers a vector; `ingest` claims a queue message and calls `/api/internal/ingest`, where the maths already lives. A Deno import map at `packages/core/src` is only needed if one ever computes a figure of its own. |

## How to change this file

1. Find the row. If there is none, the feature is new: add a row with both
   platforms' status before writing code.
2. Move the status in the same commit as the code.
3. `partial` and `n/a` carry a reason in Notes, always.
4. Never leave a `planned` row pointing at a phase that has closed.
